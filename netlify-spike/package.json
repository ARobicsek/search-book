{
  "name": "searchbook-netlify-spike",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "THROWAWAY de-risk spike for the SearchBook Netlify migration (Phase 0). Not part of the app.",
  "scripts": {
    "generate": "prisma generate",
    "build": "prisma generate"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "@prisma/adapter-libsql": "^7.5.0",
    "@prisma/client": "^7.5.0",
    "openai": "^6.34.0"
  },
  "devDependencies": {
    "prisma": "^7.5.0"
  }
}
