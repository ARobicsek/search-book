# Local development (SQLite)
DATABASE_URL="file:./dev.db"

# Production (Turso) - set in Vercel dashboard
# TURSO_DATABASE_URL="libsql://your-database-name.turso.io"
# TURSO_AUTH_TOKEN="your-auth-token"

# Production (Vercel Blob for photos) - auto-set when you enable Blob in Vercel dashboard
# BLOB_READ_WRITE_TOKEN="vercel_blob_..."

# OpenAI API key for LinkedIn profile parsing (Import from LinkedIn feature)
# OPENAI_API_KEY="sk-proj-..."

# ── Outlook calendar import (meetings picker) ──
# Published Outlook calendar .ics link. Until this is set, the Import-from-Outlook
# dialog shows "Outlook calendar not connected" (icsProvider.isConfigured() is false).
# OUTLOOK_CALENDAR_ICS_URL="https://outlook.office365.com/owa/calendar/.../reachcalendar.ics"
# IANA timezone the ICS wall-clock times are interpreted in. Defaults to America/New_York.
# APP_TIMEZONE="America/New_York"

# ── Web Push action reminders (all optional; reminders are off until these are set) ──
# Generate the VAPID key pair once: `npx web-push generate-vapid-keys`
# VAPID_PUBLIC_KEY="B..."           # the client fetches this at runtime from /api/push/public-key
# VAPID_PRIVATE_KEY="..."
# VAPID_SUBJECT="mailto:ari.robicsek@gmail.com"
# Secret the external cron uses to call /api/cron/reminders. Dedicated so it never has to
# share/expose the backup's CRON_SECRET (falls back to CRON_SECRET if this is unset).
# REMINDERS_CRON_SECRET="pick-a-long-random-string"
# Owner timezone for converting an action's wall-clock dueDate+dueTime into a real instant.
# REMINDER_TZ="America/New_York"
