# Session end

The session protocol is single-sourced and agent-agnostic. Follow the **"Session end"**
section of **`AGENTS.md`** (repo root). It lists the STATUS / handoff / decisions updates and the
commit-and-push step.
