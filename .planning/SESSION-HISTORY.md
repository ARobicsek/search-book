# SearchBook — Session History & Decision Archive

The **full historical record**: every durable decision ever made plus the per-session log. This is
the archive — for the *current, in-force* subset of decisions see [STATE.md](STATE.md); for the
session protocol see `AGENTS.md` at the repo root.

> Going forward: when a session makes a durable decision, add it to **both** the current table in
> `STATE.md` (if still in force) **and** the full ledger below. When a decision is later superseded,
> it stays here but drops out of `STATE.md`.

---

## Decision Ledger (full history)

| Decision | Choice | Date |
|----------|--------|------|
| Interface | Browser-based local web app | 2026-02-02 |
| Tech stack | React+Vite, Express, SQLite/Prisma, shadcn/ui, FullCalendar | 2026-02-02 |
| Data storage | Local SQLite DB, backup to Google Drive | 2026-02-02 |
| Calendar | Standalone in-app (no Google Calendar sync) | 2026-02-02 |
| Companies | Separate entity (not a contact type) | 2026-02-02 |
| Categories | 6 fixed ecosystems + custom freeform tags | 2026-02-02 |
| Contact statuses | NEW, RESEARCHING, CONNECTED, AWAITING_RESPONSE, FOLLOW_UP_NEEDED, LEAD_TO_PURSUE, ON_HOLD, CLOSED | 2026-02-06 |
| Photos | Drag-drop upload (JPG/PNG) + URL paste | 2026-02-02 |
| Date flexibility | Support day, month, or quarter precision for historical entries | 2026-02-02 |
| Recurring tasks | Supported with configurable intervals | 2026-02-02 |
| Data entry | Quick-add palette + structured forms + CSV bulk import | 2026-02-02 |
| Search | By name, role, company, date of contact, keywords, ecosystem, status | 2026-02-02 |
| Navigation | Collapsible left sidebar with icons + labels | 2026-02-02 |
| Table library | TanStack Table (with shadcn DataTable recipe) | 2026-02-02 |
| Dashboard | Daily view (home page), weekly stats in Analytics | 2026-02-03 |
| Deployment | Vercel + Turso cloud DB for iPhone PWA access | 2026-02-05 |
| API auth | Single shared-password gate over all `/api` routes (single-user app) | 2026-06-03 |
| Cloud backup | Automated daily DB export to Vercel Blob (`/api/backup/cron`, 08:00 UTC, keep newest 30) | 2026-06-03 |
| Photo backup | Actual photo *files* bundled into the **manual** backup ZIP only (not the daily cron, to keep Turso/cloud backups small); automatic layer is best-effort by design (Task 25) | 2026-06-03 |
| LLM search-agent export | Manual backup emits a **third file**, `searchbook-notes-<stamp>.md` (client-side from the same in-memory export; `client/src/lib/llm-export.ts`, called in `settings.tsx handleBackup` beside the JSON + binaries ZIP). Markdown **prose corpus** for an LLM search/synthesis agent — Meetings (newest-first, per-attendee takeaways) / People (+ dossier prep notes) / Organizations / Ideas (archived incl. + labeled) — every ID resolved to a name, note markup cleaned (`@Name`, `[image]`). Routing: `.md` is the default for prose/theme/ideas/notes queries (complete prose corpus, no silent cross-file miss); JSON stays restore-of-record + structured-fact fallback; ZIP holds binaries. Guide `.planning/SEARCH-AGENT-GUIDE.md`; format source of truth = `llm-export.ts`. Manual-backup only (not the daily cron). Schema-free, client-only | 2026-07-16 |
| API caching | `/api/` is `NetworkOnly` in the service worker — never cache API responses (prevents stale data overwriting newer via auto-save) | 2026-06-04 |
| PWA updates | `registerType: 'prompt'` so the user is offered new bundles (not silent `autoUpdate`) | 2026-06-04 |
| Rate limiting | `express-rate-limit`: 1000/15min on `/api` (before auth gate; skips `/health`), 40/hr on `/api/linkedin`; body limit 50mb→2mb (backup routes keep 50mb) | 2026-06-04 |
| Error tracking | Opt-in Sentry (`@sentry/node` + `@sentry/react`), no-op until `SENTRY_DSN`/`VITE_SENTRY_DSN` set; wired into the React ErrorBoundary | 2026-06-04 |
| CORS | Exact allow-list (localhost + prod domain), no `*.vercel.app` wildcard; header-auth is the real gate | 2026-06-04 |
| App mission | Adapt SearchBook (vs. replace) into NCQA CMO stakeholder-management system; plan of record = `.planning/NCQA-ADAPTATION-PLAN.md` | 2026-06-12 |
| Legacy data | No archiving; legacy ecosystem values stay valid (additive taxonomy), reclassify opportunistically | 2026-06-12 |
| Meetings model | Conversation = meeting with title + 4 optional "who" facets (contact anchor, org anchor, named participants w/ per-person notes, free-text attendees description; ≥1 of title/facets required); multi-subject via markdown `### Topic` headings + conversation tags, NOT per-topic child records | 2026-06-12 |
| Recurring meetings (D4) | ~~Series identified by repeated, autocompleted `title`~~ **REVISED 2026-06-15: dedicated `Series` entity** (`Series` table + `Conversation.seriesId`). Series is **opt-in** — mark a meeting as a series, later meetings join it from a picker (no exact-title retyping). The `series` chip shows only for meetings actually in a series. Migration auto-grouped existing titles shared by ≥2 meetings. Still **no Groups feature** (deferred to backlog) | 2026-06-12 / 2026-06-15 |
| Meetings sort + `updatedAt` | `Conversation.updatedAt` (`@updatedAt`) added; `/api/meetings` takes `sortBy` (date/updatedAt/createdAt) + `sortDir`. **Default sort = recently updated** (most recent on top). Caveat: `updatedAt` bumps on edits to the Conversation row/junctions, NOT on isolated child-record edits (prep note / attachment) | 2026-06-15 |
| List Table Sorting | Contacts list uses server-side sorting for all columns (`manualSorting: true` in TanStack Table + `ORDER BY` pushed to `/api/contacts`) due to server-side pagination. Other lists (Organizations, Actions, Ideas) keep client-side sorting because they fetch unpaginated full datasets into memory, making client-side sorting instantaneous and correct. | 2026-06-16 |
| Meeting card title + participant order | Display precedence `title → first participant → contact → company → attendeesDescription`. "First participant" needs an explicit order: added `ConversationParticipant.ordering` (set from the submitted array index on create; `orderBy` in the meeting/conversation includes) — without it, participants returned in `contactId` order, so the title was effectively arbitrary. Legacy rows backfilled by per-conversation rowid (insertion-order proxy). ~~Search box also ranks title/participant~~ **REVISED 2026-06-16: Meetings search box matches the TITLE only** (see below) | 2026-06-15 |
| Meetings search = heading text | **Owner override (2026-06-16) of the earlier "rank title + participant" behavior:** the Meetings Search box matches the meeting's displayed HEADING — `Conversation.title` (contains), or for **untitled** meetings the name shown in its place (first participant → anchor contact → org → attendees text, mirroring `conversationDisplayName`). Titled meetings are matched by title only; notes/summary/tags stay out (they have their own filters). Removed the JS relevance-rank path (search now respects the chosen sort). Global `/api/search` unchanged (still all-fields). Term highlighted only in the card heading. **REVISED 2026-06-16 (later, same day): the box now also matches any named PARTICIPANT and the meeting's SERIES name — for ALL meetings, not just untitled ones (`{participants:{some:...}}` + `{series:{name:contains}}` lifted out of the untitled gate); the untitled-only fallback now covers just anchor contact / org / attendees. The card now highlights the matched term in the heading, the **series chip**, AND **participant badges** (shared `hl()` helper in `MeetingCard` applied to all three, mirroring the q match).** | 2026-06-16 |
| Meetings org filter = org field + current employees | The Organization filter on the Meetings page now matches a meeting when the company is its anchor/additional org **OR** when the meeting's anchor contact / any named participant CURRENTLY works there (shared `currentEmployerCompanyIds` rule: primary `companyId` + `additionalCompanyIds` not marked `isCurrent:false`). Implemented in `meetingOrgClauses()` — prefilter candidate contacts by cheap substring match, confirm in JS, expand the meeting `OR` with `contactId in [...]` + `participants.some`. Single-user scale → sub-second (owner's 20s ceiling not at risk); if a future dataset made it slow we'd drop the employee expansion, not page it | 2026-06-16 |
| Meeting cards collapsible | List/series meeting cards render compact: content clamped to ~`COLLAPSED_MAX_PX` (168px ≈ 2in) with a fade + "Show more"/"Show less" toggle when it overflows; clicking a clamped card expands it (clicks on inner links/buttons are excluded via `closest('a,button')`). Overflow detected by measuring the un-clamped inner content div with a `ResizeObserver` (handles late images/markdown). `MeetingCard` extracted as its own component | 2026-06-16 |
| "Recently updated" sort — merge bug found + fixed | Real culprit for old meetings jumping to the top of "Recently updated": **merging duplicate contacts** ran `tx.conversation.updateMany({contactId: removeId} → keepId)`, and Prisma `@updatedAt` bumped `updatedAt` to *now* on every re-linked meeting. Fixed in `duplicates.ts` by re-linking via **raw SQL** (`$executeRaw UPDATE "Conversation" SET "contactId"=…`) so a merge is a re-link, not an edit. (Ruled out as causes: the status-sweep scripts — `Company`/`Contact` only; editing a contact card — the contacts route never writes conversations; raw-SQL migrations/cascade-deletes — don't trigger `@updatedAt`.) **Forward-only fix:** meetings whose `updatedAt` a *past* merge already bumped stay bumped (can't tell merge-bumps from real edits to safely reset). Separately, `updatedAt` legitimately = when a meeting was logged or edited, which differs from the meeting `date` shown on the card | 2026-06-16 |
| Taxonomy (D1–D3) | Ecosystems: new NCQA list + keep RECRUITER; ROLODEX/TARGET/INFLUENCER/INTRO_SOURCE→NETWORK. Contact statuses: RESEARCHING/CONNECTED/AWAITING_RESPONSE/FOLLOW_UP_NEEDED + blank. Company: RESEARCHING/ENGAGED/PARTNER/CONNECTED + blank. Blank = `'NONE'` sentinel (no table rebuild) | 2026-06-12 |
| Meeting capture | Paste MS Copilot recaps → Claude API extraction → mandatory review screen → normal CRUD (AI never writes directly) | 2026-06-12 |
| Calendar | Outlook via published ICS feed (server-side secret env var), v1; Graph OAuth only if ICS staleness hurts | 2026-06-12 |
| Git workflow | Owner granted standing permission to commit/push directly to `main` (prepush typecheck first; Turso DDL before schema code) | 2026-06-12 |
| Meeting prep notes & attachments | Per-conversation tables (`ConversationPrepNote`, `ConversationAttachment`); advance prep = future-dated meeting + prep notes; attachments ≤4MB via server pass-through (Vercel Blob `files/` prefix in prod, `server/data/files/` in dev); binaries excluded from daily DB backup (photo precedent) | 2026-06-12 |
| Meeting editor | Quick Log dialog is the canonical create+edit meeting editor app-wide (`useQuickLog().openEdit(id)`); contact-detail keeps its legacy embedded editor for now | 2026-06-12 |
| Restore test (Item 5) | ✅ Executed & PASSED: real prod backup restored into a scratch Turso DB via `server/scripts/restore-test.mjs` — 27/27 tables (2,604 rows), 15/15 sampled Blob URLs reachable, prod untouched. Backup→restore round-trip is now empirically proven; re-run the runbook against future backups if the schema/backup format changes | 2026-06-14 |
| Backup completeness proof | ✅ Backup confirmed a FULL, working copy of prod: read-only prod↔backup `count(*)` diff (`server/scripts/prod-count-diff.mjs`) = **all 27 tables identical, delta 0, 2,604 rows**; backup also restored into local SQLite + app booted on it (`server/scripts/app-smoke.mjs`, every page/chart rendered). Reusable verification scripts kept alongside `restore-test.mjs` | 2026-06-14 |
| Meeting search result → expanded detail | Clicking a **meeting** result in global search opens a **read-only `MeetingDetailDialog`** (full contents + prep notes + attachments + related chips), fetched via `GET /conversations/:id` (search only carries 60-char snippets), with the search term(s) **highlighted throughout** — plain text via `HighlightedText`, rendered markdown via the `highlightRehype` plugin (`MentionableMarkdown` gained optional `highlightTerms`/`caseSensitive`). Its **"Edit meeting"** button opens the canonical Quick Log editor for that meeting via `useQuickLog().openEdit(id)` (replaced the first cut's link to `/meetings?id=`, which just showed the whole list). Schema-free, client-only (`182885e` feat, `28fb55a` edit-button fix). Command-palette meeting hits still navigate to the list (out of scope) | 2026-07-04 |
| Undo last delete | Server-side **snapshot-and-replay** (`DeletedSnapshot` table + `server/src/lib/undo.ts`): each delete first captures cascade rows (recursive) + SetNull'd FKs + company JSON-array scrubs atomically with the delete; `POST /api/undo` replays the most recent (small stack, pruned to 25). Chose this over soft-delete (would touch every read query) and client-deferred-delete (fragile on PWA). Persistent client command: header Undo button + Cmd/Ctrl+Z, `GET /api/undo`-backed so it survives reload; content area remounts after undo (`key` on `<main>`) to show the restored item. Attachment deletes no longer destroy the Vercel Blob (so restorable); backup wipe clears snapshots. Plan: `archive/UNDO-DELETE-PLAN.md` | 2026-06-16 |
| Markdown speed input | Shared `MarkdownTextarea` (toolbar + Ctrl+B/I, Ctrl+Shift+8/7, Ctrl+Alt+1-3, Enter list auto-continue, paste-screenshot→upload→`![](url)`) instead of a rich-text editor | 2026-06-12 |
| Markdown in Actions/Ideas (A1) | `MarkdownTextarea` + `ReactMarkdown`/`prep-note-markdown` applied to **description** fields only (titles stay plain) | 2026-06-14 |
| Action form disclosure (B1) | Type/Priority + "Related To" collapsed behind carets (Quick Log "Who was there" pattern), on **both** create + edit; collapsed fields stay in form state | 2026-06-14 |
| "Who owes it" model (C1, shipped) | Additive `Action.owedByMe` (bool, default 1) + `Action.owerContactIds` (JSON id array); `direction` enum kept **derived server-side** (`owedByMe && owers empty ? OWED_BY_ME : WAITING_ON_THEM` — the real enum value is `WAITING_ON_THEM`, **not** `OWED_TO_ME` as earlier prose mistakenly said) so dashboard "Waiting on others" / `?filter=waiting` / list+detail badges read it unchanged. Form: collapsed "Who owes it" disclosure (removable default-on **Me** chip + contacts MultiCombobox + favorite quick-add, Quick Log pattern). Server `resolveOwers()` also back-compat-accepts a bare legacy `direction`. Turso DDL (2 additive `ADD COLUMN` + backfill) applied via dashboard 2026-06-14. **Display refinement (2026-06-18, `b1f6bc5`):** compact action cards (dashboard `ActionRow`, Actions list "Contact" column incl. sort+search) + the detail page now show the **ower** (the person you're waiting on), not the "Related To" contact — server-side `attachOwers()` resolves `owerContactIds`→`owers` `[{id,name}]` in one batched query (derived field, no schema change), client `actionDisplayPeople()` prefers owers else related contact(s) | 2026-06-14 |
| Company near-dup detection (D1) | Punctuation normalize (&→and, hyphen→space, drop apostrophes/diacritics) + `healthcare`→`health` + trailing-descriptor strip + token-subset; **plus** a low-confidence shared-prefix bucket (≥3 non-stopword shared leading tokens, score 0.5) so divergent-tail same-parent pairs (Baylor Health vs Research Institute) surface for review without flooding. Favor recall (review-then-merge) | 2026-06-14 |
| Search upgrade | Plan of record `archive/SEARCH-UPGRADE-PLAN.md`: full-field coverage, scope groups (People-profile / People-notes / Orgs / Meetings / Actions / Ideas), multi-term AND, 4 sorts, match snippets; plain `LIKE`, no FTS5 at current scale | 2026-06-12 |
| Case-sensitive search | `caseSensitive=true` param (user ask, default off): DB fetches the insensitive `LIKE` superset, JS verifies exact case during snippet computation; totals in cs-mode are the verified count of the fetched superset | 2026-06-12 |
| Favorite contacts | Stored as a reserved `Favorite` tag via the existing `ContactTag` junction (no schema change, synced, in backups); `GET /contacts/favorites` + `PATCH /contacts/:id/favorite`; UI = star toggle + quick-add chips in the Quick Log participants block | 2026-06-12 |
| Multi-org meetings | New `ConversationOrg` junction = orgs the meeting was WITH (anchor `companyId` stays primary, same pattern as `Contact.additionalCompanyIds`); distinct from `ConversationCompany` (orgs *discussed*). Org filter + search match both | 2026-06-12 |
| Connect → company status | When a contact becomes CONNECTED (create-as-connected, status change via PUT, or linked EMPLOYED via `POST /companies/:id/contacts`), their **current** employer(s) auto-promote to company status CONNECTED. Guarded: only promotes from NONE/RESEARCHING — never downgrades ENGAGED/PARTNER/CONNECTED; past employers (`isCurrent=false`) skipped. Records a `CompanyStatusHistory` row. Shared helper `server/src/company-status.ts` (no schema change) | 2026-06-15 |
| Ideas card screenshots | Collapsed Idea cards hide markdown images (`[&_img]:hidden`) + show a "N screenshots — click to view" hint so pasted screenshots don't balloon card height; expanding shows full markdown + full-size images | 2026-06-15 |
| Quick Log Summary field | Hidden behind a caret (same pattern as "Who was there"/"Actions…"), collapsed by default; auto-expanded in edit mode when the meeting already has a summary | 2026-06-15 |
| Quick Log prep notes | Autosave as you type (composer POSTs draft on first pause then PUTs; "+ New note" splits; serialized + flushed on blur/Done so no lost keystrokes / no double-create). Saved notes render **formatted markdown** by default; click body or pencil to edit in place (raw textarea, still autosaving), ✓/blur to return. Editable in edit mode too. `MarkdownTextarea` gained an optional `onBlur` | 2026-06-15 |
| Quick Log mobile layout | Side-by-side prep panel is **desktop-only** (`useIsDesktop` matchMedia `min-width:640px`); on mobile the prep list+composer render full-width inside the form, and the "Actions, prep…" section auto-expands in edit mode when notes exist (so they're not buried) | 2026-06-15 |
| Radix Dialog + resize handle | Dragging the prep/form divider was closing the dialog: react-resizable-panels' native pointerdown pre-empts Radix's "inside" marker, so Radix mis-reads the handle as an *outside* interaction even though `content.contains(handle)`. Fix: guard `DialogContent.onInteractOutside` — `preventDefault()` when `e.detail.originalEvent.target` is within `[data-slot="resizable-handle"]`/`resizable-panel-group`; genuine backdrop clicks still close. Reusable pattern for any Radix dialog hosting a resizable panel group | 2026-06-15 |
| Meeting editor unification | **One editor: the Quick Log dialog**, app-wide. Retire the contact-page inline `ConversationsTab` editor; logging from a contact opens Quick Log seeded with that person as a Participant; drop the "1:1 anchor" field; add autosave (create-then-PUT). Plan: `archive/UX-SEARCH-MEETINGS-PLAN.md` Phase C | 2026-06-13 |
| Search perf | ~20s search is Turso round-trip count (≈150 from `includeRelated` fan-out + sequential top-level finds), not data volume. Fix = lazy-load related on expand + `Promise.all` the independent queries. **No FTS** at this scale (text scan isn't the bottleneck) | 2026-06-13 |
| Search perf (shipped, Phase B3) | `GET /api/search` `includeRelated` now defaults **false**; per-card related loads lazily via `GET /api/search/related/:type/:id` (client caches by `${type}-${id}`); top-level finds + per-term company lookups run in `Promise.all`. Per-entity related count badge is gone (was the reason to fan out) → plain "Related" expander | 2026-06-13 |
| Meetings free-text ranking (shipped, Phase B2) | `/api/meetings?q=` covers every meeting field (mirrors search's `conversationClausesFor`) and ranks by max-weight: **title=4 > anchor/participant names=3 > anchor/addl org names + attendees desc=2 > everything else=1**, then date desc; fetch-300-then-slice JS pagination (same shape as the series-title path) | 2026-06-13 |
| LinkedIn import | Stays **paste-text only** (no screenshot/AI-vision path) — owner deferred mobile import 2026-06-13 | 2026-06-13 |
| Meetings calendar | **Separate meetings-only calendar** (List\|Calendar toggle on `/meetings`), distinct from the actions calendar; click an event → open in Quick Log; future-dated meetings = advance prep | 2026-06-13 |
| Meeting editor unification (shipped, Phase C) | Contact-page inline editor **retired**; Quick Log is the **sole** meeting editor. `useQuickLog().open({ participant?, title? })` seeds a contact as a Participant; "Who was there" auto-expands. **Autosave** = numeric-only body (scalars + already-resolved participants/orgs/tags) via POST-once-then-PUT through a serialized save chain; **never** sends `contactId` (preserves legacy anchors) or `createActions` (a PUT re-creates those). Free-text names + follow-up actions persist only on the explicit **"Done"** finalize; prep/attachments persist live once the record exists. A "meaningful content" gate stops a pre-seeded participant alone from auto-creating an empty meeting. The "1:1 anchor" field is gone — people via Participants only. Display fallback adds first-participant before attendees | 2026-06-14 |
| Meetings free-text highlight (shipped, Phase B4) | The Meetings `q` filter highlights matches in plain-text fields (display name, summary, attendees desc, next steps, participant/org/tag badges) via a shared `HighlightedText` ([client/src/components/highlighted-text.tsx](../client/src/components/highlighted-text.tsx)) reused by Search; `notes`/prep markdown bodies stay un-highlighted (no `<mark>` into raw markdown) | 2026-06-14 |
| Meetings calendar (shipped, Phase D) | List\|Calendar toggle on `/meetings` (`?view=calendar`); meetings-only `MeetingsCalendar` (FullCalendar dayGrid+list) fetches **only the visible range** via `/api/meetings?from=&to=&limit=100` on `datesSet`, refetches on `searchbook:meeting-logged`. All-day events titled by `conversationDisplayName` (now participant-first), colored by type via a **hex** map (FullCalendar needs CSS colors, not Tailwind classes). Event click → `quickLog.openEdit(id)`. Mobile defaults to list view via a one-frame `ready` mount gate (so FullCalendar reads the resolved `useIsMobile` for `initialView`). Filters hidden in calendar view; series/`title` view forces list | 2026-06-14 |
| Long-lived PrismaClient (shipped, Task 5) | Retired the per-request `resetPrisma()` middleware (the 2026-03-24 "fresh client per request" fix). `db.ts` now keeps **one** `_client`, reused across requests; a deepened `Proxy` routes every query through `runWithRetry`, which rebuilds the client + retries **once** only on a connection/transport error (`isConnectionError`: Prisma `P1xxx` + `ECONNRESET`/`fetch failed`/`stream closed`/… but never `P2xxx`, so a landed write is never double-applied). Concurrent failures rebuild once (`_client === client` guard). Safe because all `$transaction` usages are the callback form (lazy `PrismaPromise` preserved). Verified on the live Turso-on-Vercel deploy across two idle cycles (6m + 9m): all `/api/health` probes `200`, zero 5xx, first-after-idle reconnects transparently | 2026-06-14 |
| Terminology "Meetings" (shipped, Phase E) | User-facing "Conversation(s)" relabeled to "Meeting(s)" app-wide (contact tabs/Prep Sheet/delete copy, action detail, analytics chart, search heading, merge dialogs). **UI strings only** — `Conversation` model/types, `/conversations` API, event names, and `draft_*conversation*` localStorage keys unchanged (mirrors the Companies→Organizations relabel) | 2026-06-14 |
| Actions calendar unified (shipped) | The standalone Calendar page/left-bar item is **retired**; the actions calendar is now a **List\|Calendar toggle on `/actions`** (`?view=calendar`), mirroring Meetings. `calendar.tsx` exports a reusable `<ActionsCalendar>`; `/calendar` redirects to `/actions?view=calendar`. Waiting-on actions get a ⏳ title prefix + native "Waiting on: <names>" tooltip. Both calendars blank FullCalendar's mobile "all-day" label (`allDayText=""`) | 2026-06-14 |
| "Owned by" wording | User prefers "owns/owned" over "owes/owed" in Actions — display strings only ("Who owns it", chips, dashboard waiting-card copy). Schema field `owedByMe` + enum `OWED_BY_ME`/`WAITING_ON_THEM` are internal and unchanged | 2026-06-14 |
| Idea archive (shipped) | Soft-archive via additive `Idea.archived Boolean @default(false)` (single `ADD COLUMN`, no rebuild). `GET /api/ideas?archived=only\|all` (default = active only); **dedicated** `PATCH /api/ideas/:id/archive` so archiving never hits the PUT junction-rebuild (which would wipe contact/company links). Client: Active/Archived/All lozenges + per-card Archive/Unarchive; archived hidden by default, searchable only when opted in. Turso `ALTER TABLE "Idea" ADD COLUMN "archived"` applied by owner via console 2026-06-14; commit `cce0f78` pushed, live | 2026-06-14 |
| Resizable free-text dialogs (D-Q1) | Owner chose **resizable dialog windows** (not textareas): desktop-only `sm:resize` + `overflow-auto` + min/max on the Idea and Quick Log `DialogContent`s; the textarea reflows to fill. Mobile untouched. (CSS `resize` on the centered `translate(-50%,-50%)` dialog keeps the drag handle under the cursor — it grows symmetrically from center) | 2026-06-14 |
| Ideas-scoped search | Ideas page search stays **Ideas-only** (never the global multi-entity search): adds sort (Relevance/Newest/Oldest/A→Z) + match-case + multi-term AND over title/tags/related-names/desc + `HighlightedText` on plain-text fields (markdown desc un-highlighted, Meetings precedent). All client-side over the loaded list | 2026-06-14 |
| Idea tags → app-wide Tag (owner decision) | Owner chose to **share Idea tags with the app-wide `Tag` vocabulary** (Meetings/Contacts) so reuse + autocomplete span the app. Additive schema: new `IdeaTag` junction (single `CREATE TABLE`, no rebuild); legacy `Idea.tags` string kept + backfilled. Server includes `tagLinks` (shape mirrors `Conversation.tags` → `idea.tagLinks[].tag`), accepts `tagIds`; partial PUT w/o `tagIds` leaves tags untouched. Client tag input = free-text `MultiCombobox` from `GET /tags`, `resolveTagIds` creates new tags on save (idempotent by name, **also during edit-mode autosave** — unlike contacts/companies — since building the shared vocab is the point). Migration `migrate-ideas-tags-to-junction.js` (dual-mode local/Turso). Commit `c3f18dd` — **LIVE**: owner applied the Turso `CREATE TABLE "IdeaTag"` + recursive-CTE comma-tag backfill via the web console, then pushed | 2026-06-15 |
| Reserved Favorite tag hidden from pickers | `GET /tags` excludes the reserved `Favorite` tag (`FAVORITE_TAG_NAME`, the internal mechanism for starred contacts/companies — not a user tag) so it never appears as a selectable tag in any picker (Ideas/Meetings filter/Quick Log/contact-detail). Surfaced once Ideas joined the shared tag vocab. Favorites themselves are unaffected (they query the tag relation directly via `/contacts|companies/favorites`). Commit `bd1fbb7` | 2026-06-15 |
| Meeting title → Edit (owner decision, ask #9) | Clicking a meeting's list heading opens the Quick Log **Edit** dialog (`quickLog.openEdit`); a small **"series"** chip (only for titled meetings) preserves the grouped `?title=` view. Anchor-contact chip now renders for **all** contact-anchored meetings (not just titled) so contact nav isn't lost when the heading becomes an edit trigger | 2026-06-15 |
| Quick Log actions autosave (asks #5/#6/#7) | Composer "Follow-up actions" rows **autosave as real Actions** once the meeting record exists — POST `/actions` w/ `conversationId`, then debounced PUT (mirrors prep-note autosave). Dedup via a **synchronous** `savedActionsRef` (Map keyed by a stable row `key`) so a debounce + finalize reconcile can't double-create; remove deletes the Action. Per-row **"Who owns it"** picker (`owedByMe` + `owerContactIds`, Actions-form pattern). "Add action" is the solid primary button. Conversation create/update no longer sends `createActions` (server path left for back-compat) | 2026-06-15 |
| Ideas List view + density | Ideas page gets a **Card/List toggle** (persisted `localStorage.ideas_view`); List = dense click-to-expand rows. Card default spacing trimmed (`gap-6 py-6` → `gap-2 py-3`). Shared render helpers (action buttons / tag chips / related chips) used by both views | 2026-06-15 |
| Markdown body highlight | Ideas search now highlights matches **inside** the rendered markdown description via a self-contained rehype plugin ([client/src/lib/highlight-markdown.ts](../client/src/lib/highlight-markdown.ts)) that wraps text-node matches in `<mark>` (skips code/pre) — supersedes the earlier "markdown desc un-highlighted" stance **for Ideas**. `HighlightedText` still handles plain-text fields. Reusable for Meetings notes later. Next Steps in meetings is now a `MarkdownTextarea` + rendered markdown | 2026-06-15 |
| "Useful people" (useful-for) | Owner-requested affordance to recall *who could help NCQA with topic X in future*. **Field-driven, deliberately NOT a tag** (owner's call): additive `Contact.usefulFor` free text = "what this person could help with"; **non-empty ⇒ a useful person** (single source of truth — chose presence-of-text over a reserved tag + field to avoid the two drifting). Capture = "Useful For" card on the contact **form**; display = amber 💡 card on contact **detail** (shown when set). Recall = Contacts-list **Useful** filter button (`?useful=true`, server `where.AND:[{usefulFor:{not:null}},{not:''}]`, composes with the search `OR`) **+** `usefulFor` folded into the list `search` OR, so "Useful + type a topic" narrows to matching people; 💡 marks useful rows. Global `/api/search` gives `usefulFor` its **own dedicated scope** (`useful`, chip "Useful for") — searchable **in isolation** ("who is useful for <topic>"), separate from People — notes; weight 2; a contact query now runs if any of the three people scopes is on (`anyPeople`). Result cards **flag a useful-field hit** with an amber 💡 + "useful for:" label (`MatchEvidence`). **Contact merge** (`duplicates.ts`) unions `usefulFor` from both sides (kept text first, removed appended only if it adds something new) regardless of the merge field-selections — a useful person is never dropped, whichever side is removed (`unionUsefulFor`). Migration `migrate-contact-usefulfor.js` (dual-mode); Turso `ALTER TABLE "Contact" ADD COLUMN "usefulFor" TEXT` applied by owner 2026-06-16 | 2026-06-16 |
| Search scope UX (owner ask) | Scope badges **default all-on every visit** — scope selection is no longer persisted in `searchbook_search_prefs` (sort + match-case still are); added an **"All" reset chip**. Click model: from the all-on default a single click **ISOLATES** to that one scope; a **second click on that lone scope INVERTS** to everything-except-it; otherwise plain add/remove on the current subset (≥1 kept). URL `?scopes=` still wins so shareable/deep narrowed links keep their selection. ("Useful for" is one of the always-on scopes) | 2026-06-16 |
| Series management UI | `/api/series` rename/delete already existed but was only reachable via easy-to-miss icons in the series-view header → added a discoverable **"Manage" link beside the Meetings Series filter** opening `ManageSeriesDialog` (every series w/ meeting count + last date, inline rename, delete-with-confirm; deleting the currently-viewed series clears the filter). Header icons kept | 2026-06-16 |
| Contact card data entry (owner ask) | Photo box now **accepts clipboard paste** (page-level `paste` listener active only while no photo is set; ignores pastes targeted at inputs/textareas so it never hijacks the notes editor — shared `<PhotoUpload>` has a single instance). Contact **Notes box upgraded to `MarkdownTextarea`** (formatting toolbar + paste/drag screenshots; detail page already renders notes as markdown) | 2026-06-16 |
| Quick Log prep bar on caret | Expanding the desktop "Tags, prep notes & attachments" section now opens the left **Prep Notes** bar immediately (added `showTagsPrep` to `showPanel`), instead of only appearing after a prep note was saved + the dialog reopened. Prep notes move to the left bar; the section keeps Tags + Attachments. Mobile unchanged (no side panel) | 2026-06-16 |
| New Organization default status | New-org form status now defaults to **blank (`NONE`, "—")** instead of `RESEARCHING` (owner ask) | 2026-06-16 |
| Outlook meeting import (D7 resolved) | Fail-fast probe (`server/scripts/probe-ics.mjs`) proved NCQA M365 **can** publish an ICS feed (847 events, subject/date/time/recurrence intact) but **Microsoft strips attendees** (0/847). Owner chose **Option A**: import the calendar *skeleton* as future-dated meetings, add attendees manually; keep Option B (Graph/Power-Automate attendee auto-fill) open behind a `CalendarProvider` interface. Shipped: additive `Conversation.calendarUid` + `startTime`; `server/src/lib/ics.ts` (`IcsCalendarProvider` — fetch + 15-min cache + `ical-expander` recurrence expansion + Windows-TZID→`APP_TIMEZONE`; collapses ical-expander's master/occurrence duplicate per `uid|date`); `server/src/routes/calendar.ts` (`GET /events` annotated with `alreadyImported`, **skip-only idempotent** `POST /import` keyed `calendarUid`+`date`, env-gated on `OUTLOOK_CALENDAR_ICS_URL` → clean 503); polished "Import from Outlook" dialog (range presets, day-grouped, pre-selects not-yet-imported, remembers last range). `startTime` shows on cards + editable in Quick Log. `/api/calendar` exempt from the 12s soft-timeout | 2026-06-17 |
| Optimistic-concurrency guard | Enforce the cross-device "stale write" check (contact/company/action PUT) by comparing `updatedAt` **epoch-ms in app code** (`assertNotStale`), NOT via a DB-level `where: { updatedAt: <Date> }` filter. Prisma 7's datetime equality only matches rows it last wrote (`...+00:00` text); restore/import/raw-SQL rows (`...Z`) never matched → permanent false 409s. App-code comparison is representation-independent | 2026-06-19 |
| Action reminders (time-of-day + Web Push) | Additive `Action.dueTime`/`notify`/`lastNotifiedAt` + new `PushSubscription` table; `dueDate` stays date-only. Time and alert **independent** (alert w/o time → 09:00 America/New_York). Free Web Push (VAPID) + free external 1-min cron → `/api/cron/reminders` (dedicated readable `REMINDERS_CRON_SECRET`, falls back to `CRON_SECRET`) converts wall-clock due moment in `REMINDER_TZ` to an instant, fans out to all subs, sets `lastNotifiedAt` once, prunes dead subs; editing date/time/notify re-arms. Client: time + bell in date popover & form, Settings→Notifications per-device enable, `push`/`notificationclick` in `public/push-sw.js` via Workbox `importScripts` (caching unchanged). Dashboard sorts today's timed actions to top by time, shifts to Overdue once past. No paid services. Runbook `.planning/ACTION-REMINDERS.md` | 2026-06-24 |
| Meeting participants — instant contacts | Adding a participant in the Quick Log dialog creates the Contact **immediately** (typed-in `POST /contacts` inline, free-text→id swapped in place) instead of deferring to "Done"; participant names in the editor become **links to the contact card**. **Bulk paste** an Outlook recipient list (`Name <email>; …`) into the field → `POST /api/contacts/resolve-participants` matches each by email (primary/`additionalEmails`, case-insensitive) → exact name → else creates (CONNECTED/NETWORK, email saved), ids merged deduped; `MultiCombobox` opt-in `onBulkPaste` intercepts only list-shaped pastes. **Auto-cleanup:** a contact created via this field, removed again before gaining other info, is deleted (per-session tracking; matched contacts never tracked). Schema-free | 2026-06-29 |
| Reuse a series' prep notes in the next meeting | The desktop Quick Log "Last Meeting in Series" panel surfaces the prior meeting's **prep notes** (not just summary/notes/next-steps) with a **"Copy to prep notes"** button that duplicates their content into the new meeting as fresh, editable prep notes — a **one-way content copy** that never touches the prior meeting's own prep-note records (dated today; staged as `pendingPrepNotes` in create mode, `POST`ed to `/conversation-prepnotes` in edit mode). The source box is offered **only while THIS meeting has no prep notes of its own** (`meetingHasPrepNotes = prepNotes.length || pendingPrepNotes.length`) — copying (or writing any) hides it **durably**, so it stays hidden after save + reopen (base signal is the meeting's own prep notes, **not** session state — that was the first-cut bug where the box reappeared on reopen), freeing the panel's vertical space for the notes box. Desktop-only (`useIsDesktop`-gated panel). Schema-free, client-only (`quick-log-dialog.tsx`) | 2026-07-02 |
| Duplicate dismissals + merge rules (persistence) | Persist dismissed duplicate pairs + "always combine" merge rules server-side (new `DismissedDuplicate` + `DuplicateMergeRule` tables) instead of per-browser `localStorage` (never synced across devices). **Keyed by normalized name, not row id** so decisions survive a reimport (new ids). Duplicates scan auto-merges pairs matching a merge rule (reimported "ABC-D" folds into "ABC") and hides pairs matching a dismissal; **merge rules outrank dismissals** (rule check first), and merging deletes any stale dismissal. `GET /api/duplicates[/companies]` now returns `{ pairs, autoMergedCount }`; both tables added to both backup paths (30→32 tables). **New tables — Turso DDL applied before deploy** | 2026-06-29 |
| Recurring actions — weekday-only mode + reminder carry-over | Recurrence stays interval-based (`recurringIntervalDays` = every N days, optional `recurringEndDate`); added additive **`Action.recurringWeekdaysOnly`** (bool) = recur on **every weekday Mon–Fri, skipping Sat/Sun** (interval ignored in that mode — a fixed day count can't express Fri→Mon). Next occurrence is auto-created **on completion** (`PATCH /actions/:id/complete`) and now carries the reminder (`dueTime`, `notify`) **and** the new weekday flag forward; `lastNotifiedAt` left null so the cron arms a fresh reminder for the new occurrence. Fixed a pre-existing bug where occurrences silently dropped `dueTime`/`notify`, so a recurring action lost its reminder after the first fire. Form UI gained a **Repeat** selector (Every N days / Every weekday); detail view shows "Every weekday (Mon–Fri)". **Additive ADD COLUMN — Turso DDL applied by owner before deploy** | 2026-07-03 |
| Hosting exit contingency (IT-driven) | If NCQA IT forces a move off Vercel: target is **Google Cloud Run** (Express + static client in one container, kept warm by the every-minute reminders cron) + **GCS** (private bucket for photos/attachments/backups, served via Express proxy on the same `/photos/` `/files/` paths dev uses) + **Cloud Scheduler** (2 of the 3 free jobs replace both Vercel cron and cron-job.org); **Turso stays**. Chosen over Cloudflare Workers (free plan's 10ms CPU cap risks hard failures for Express+Prisma; web-push/multer need porting), Render free (30–60s cold-start spin-down), Fly (free tier dead), Oracle VM (unmanaged patching burden = worse IT optics), self-host (fragile). Full phased plan with code snippets + verification gates: `.planning/VERCEL-EXIT-PLAN.md`. **Contingency only — do not execute unless the owner says so** | 2026-07-09 |
| Meeting document links | Meetings carry **Links** (URL + optional title) via the shared **`Link`** model (contacts/companies/actions already use it) through new `Link.conversationId` + `Conversation.links` (`onDelete: Cascade`). Managed in the Quick Log "Tags, prep notes, attachments & links" section (URL + label → clickable chips; live `POST`/`DELETE` once the record exists, staged + flushed on finalize before that, unadded draft URL flushed on close); `/api/links` filters by `conversationId`; `conversationIncludes` + `meetingListInclude` return `links`, shown as chips in the meeting detail dialog + `/meetings` cards. `undo.ts` cascade graph gained `link`→`conversation` so meeting-delete snapshots the links and Undo restores them (verified: delete cascades, undo restores meeting + links with original ids). Backup unchanged (`Link` already covered; `Conversation` restores first). **Additive ADD COLUMN + FK — Turso DDL applied by owner** (`ALTER TABLE "Link" ADD COLUMN "conversationId" INTEGER REFERENCES "Conversation"("id") ON DELETE CASCADE`). A same-session first cut (inline markdown Insert-link toolbar button) was reverted for this | 2026-07-10 |
| Series name stands in for the title | A meeting's **series name is its display name when it has no title** (`conversationDisplayName`: title → **series** → participant → contact → org → attendees) **and** a set series **satisfies the "≥1 who/what" save gate** — so a recurring meeting needs only series + date. Server `hasWho()` counts `seriesId` (POST + PUT); client `autosaveValid`/`hasMeaningfulContent`/finalize guard all accept it (toast → "Add a title, series, or someone who was there"). Fixes the reported bug where saving a series-only meeting still demanded a title. Schema-free (existing `Conversation.seriesId`) | 2026-07-10 |
| Combobox dropdown highlight + drop title suggestions | Active row in `Combobox`/`MultiCombobox` dropdowns highlights **light blue** (`bg-blue-100`/`text-blue-900` + dark variant) with `cursor-pointer`, matching the `@`-mention picker (replaced near-white `bg-accent`, ~invisible in Edge); scoped to comboboxes, command palette untouched. Meeting **Title** field's previously-used-titles suggestions dropdown removed (plain `Input`; `title-autocomplete.tsx` deleted). Schema-free, client-only | 2026-07-10 |
| Idea created + last-updated | `Idea` gained **`updatedAt DateTime? @updatedAt`** (nullable on purpose — unlike the non-null variant on Contact/Company/Conversation — so it drops onto the populated table via a plain additive `ALTER`, no rebuild). Prisma auto-maintains it, so the existing PUT + archive PATCH bump it with no route changes (archiving = an update; left as-is, flagged to owner). Ideas list + card views show both via shared `renderTimestamps()`: "Created {date}" always + a dimmed/italic "· Updated {date}" **only on a different-local-day edit** (same-day tweaks suppressed); hover tooltip always carries both exact timestamps w/ time. Atomic per-date `whitespace-nowrap` + wrap-between so a 390px card can't overflow; list keeps `hidden sm:inline`. Client type `updatedAt?: string \| null`. Backup unchanged. **Additive `ALTER TABLE "Idea" ADD COLUMN "updatedAt" DATETIME` + backfill `= createdAt` — Turso DDL applied by owner** | 2026-07-10 |
| LinkedIn "About" → dedicated field | The LinkedIn import's **About** text lands in a new additive **`Contact.linkedinAbout`**, no longer in `notes` (which the owner reserves for their own notes). Read-only **"About (LinkedIn)"** card on the contact detail (`contact-detail.tsx`) + editable/clearable section on the contact form (`contact-form.tsx`) render **only when the field is non-empty** (i.e. only after an import). Rendered as **verbatim pre-wrapped plain text** (not `ReactMarkdown`) to avoid mangling stray `*`/`#`/`-` in LinkedIn copy. Import dialog field-merge for About compares against a prior `linkedinAbout` (not `notes`), labeled "About (LinkedIn)". Folded into the LLM markdown export (`llm-export.ts`) so the text stays available to the search agent (previously rode inside notes). **Forward-only** — existing contacts whose About sits in `notes` are not migrated. Backup column-agnostic (`Contact` already covered). **Additive `ALTER TABLE "Contact" ADD COLUMN "linkedinAbout" TEXT` — Turso DDL applied by owner before deploy** | 2026-07-20 |

## Session Log (full history)

| Date | What Happened |
|------|---------------|
| 2026-02-02 | Initial planning session. Defined architecture, data model, features, phases. |
| 2026-02-02 | Phase 1 plan approved. Sidebar nav, TanStack Table, full-page forms, all tables upfront. |
| 2026-02-03 | Phase 1 complete. Contact/Company CRUD, list/form/detail pages, toast errors. |
| 2026-02-03 | Phase 2 complete. Action system, Dashboard, Calendar, Command palette, Ideas API. |
| 2026-02-03 | Phase 3 complete + 3 rounds of user feedback (#1-30). Conversations, Relationships, PhotoUpload, PrepSheet, PrepNotes, EmploymentHistory, roleDescription, timezone fix. |
| 2026-02-04 | Phase 4 complete. Global search+filter, CSV export/import, Tags CRUD, Ideas CRUD, date range filter. |
| 2026-02-04 | Phase 5 complete. PWA, Analytics dashboard, recurring actions, contact flagging, action history. Feedback #31-40. |
| 2026-02-04 | Phase 6 complete. Backup/restore, loading spinners, keyboard shortcuts, duplicate detection. Pre-Phase 7 fixes. |
| 2026-02-05 | Phase 7 complete. Vercel deployment, Turso cloud DB, iOS PWA meta, Vercel Blob photos. App live at searchbook-three.vercel.app. |
| 2026-02-05 | Post-Phase 7: Date precision, SPA routing fix, mobile UI (iPhone 390px), production bug fixes. |
| 2026-02-06 | PWA icons, Global Search feature, contact status enhancements, CSV import improvements, auto-save extensions, merge enhancements. |
| 2026-02-07 | Last Outreach server-side sort, duplicate detection performance (inverted-index), manual merge, Ctrl-K simplification. |
| 2026-02-08 | Browser-direct Turso backup (bypasses Vercel 30s timeout). Backup bug fixes (date parsing, path detection). |
| 2026-02-10 | Tab data indicators, direct global search, multi-select contacts/companies on actions. |
| 2026-02-12 | Quick Status/Ecosystem inline editing, action form auto-create, auto-save & drafts for conversations. |
| 2026-02-15 | Prep Notes CSS fix, Markdown rendering expansion, Company Activity Log feature. |
| 2026-02-16 | Quick Action Due Date — ActionDateSelect component with presets. |
| 2026-02-18 | Conversation save bug fix, stale search results fix, edit mode drafts with stale closure fix (useRef pattern). |
| 2026-02-23 | Inline Action Saving in conversation modal, Resume Draft bug fix, Mobile Action Due Dates. |
| 2026-02-24 | Contact draft bugs (type errors, 404 fix, duplication race condition). Analytics Page overhaul (status history, BarCharts, date picker). |
| 2026-02-25 | Analytics refinements — sparkline tooltips, drill-down pipeline, interactive charts, clickable bars. |
| 2026-02-25 | Action search includes company names, dueDate sort nulls-last, action title links in conversation cards. |
| 2026-02-28 | Conversation Participants — separate junction from "discussed", analytics drilldown updated. |
| 2026-03-04 | Log Conversation fixes — default date stale closure, modal width expansion, resizable panels (35/65 split). |
| 2026-03-05 | Timeout investigation — attempted unified endpoint, broke useAutoSave, reverted. |
| 2026-03-05 | **Timeout Root Cause & Fix.** Prisma `_count` subquery caused cascading Vercel timeouts. Stripped `_count`, added `/companies/names`, staggered loading, fetchWithRetry, non-blocking warmup. |
| 2026-03-24 | Multi-word search filtering, resilience layers (SW timeout fix, server timeout, client retry, warmup). |
| 2026-03-24 | **Query optimizations** — lighter action includes, analytics SQL aggregations, removed `_count` debug endpoint. Server timeout 25s→12s. Client retry on 500. |
| 2026-03-24 | **Turso reliability FIXED.** Root cause: `@libsql/client@0.5.6` HTTP transport hangs on large responses (170+ rows × all columns). Fix: (1) per-request fresh PrismaClient via `resetPrisma()` middleware, (2) explicit `select` on all list endpoints excluding large text fields. All endpoints now <300ms. |
| 2026-03-24 | **Prisma 6→7 upgrade.** `@libsql/client` 0.5.6→0.17.2. Adapter-based architecture (PrismaLibSql for Turso, PrismaBetterSqlite3 for local dev). Removed conditional select workaround in actions route. 171 actions now returned with full includes in production — no more response size limits. |
| 2026-06-03 | **Security hardening.** Shared-password gate over all `/api` routes (`x-app-password`), removed debug/credential leaks, hardened error output. `/health` now verifies DB connectivity. |
| 2026-06-03 | **Automated cloud backup.** Daily `/api/backup/cron` → Vercel Blob (`backups/` prefix, newest 30 kept), CRON_SECRET-gated. Settings UI lists/downloads them. Fixed export/import to cover all 23 tables (5 history/junction tables were missing). |
| 2026-06-03 | **Restore verified + `updatedAt` fix.** Isolated round-trip (seed all 23 tables → export → import → export) is now byte-identical. Fixed `/backup/import` to relink `Contact.referredById` via raw SQL so it no longer trips `@updatedAt`. NOTE: proven against local SQLite, not yet the production Turso transport (deferred to a desktop session). |
| 2026-06-03 | **Photo files in manual backup.** New `client/src/lib/photo-backup.ts` fetches actual image bytes and downloads `searchbook-photos.zip` (+ manifest) from "Create Backup". Uses `fflate`. Not in the daily cron. CORS against live Blob unverified (desktop test deferred). |
| 2026-06-04 | **Production Hardening Plan — Phase 1 complete** (Tasks 7–14, 19). Atomic restore, optimistic concurrency (409 on stale saves), autosave flush-on-nav + edit drafts + bounded retry, React error boundary, multi-write transactions, delete-impact counts, typecheck deploy gate, tags `_count`→`groupBy` Turso-hang fix. All on `main`. |
| 2026-06-04 | **Production Hardening Plan — Phase 2 complete** (Tasks 15–18, 20–25), merged to `main`. PWA `/api/` `NetworkOnly` + `prompt` updates; `express-rate-limit` + 2mb body limit; input allow-listing on company/relationship update; `safeParseArray` JSON-parse guards; dangling JSON-array ref scrub on company delete; CORS tightened to exact origins; opt-in Sentry (server + client) wired into the ErrorBoundary. Task 25 (photo backup) resolved by decision — best-effort, already covered by the manual photo-ZIP. Remaining user-action: set Sentry DSNs in Vercel to activate. |
| 2026-06-12 | **NCQA Adaptation Plan created** (`.planning/NCQA-ADAPTATION-PLAN.md`, plan of record): 6 phases — taxonomy retheme, meetings overhaul (Groups/fuzzy attendance/multi-subject), stakeholder stance+leverage, Copilot-recap AI ingest, Outlook ICS daily briefing, backlog extras. Session docs made agent-agnostic (root `AGENTS.md`, updated Gemini start/end prompts, CLAUDE.md). Decisions D1–D9 pending user sign-off. |
| 2026-06-12 | **Phase 1 built + deployed.** Taxonomy retheme (`08568e0`) + action direction/Waiting For (`71cd9b0`) on `main`; user ran the Turso migration in the web console (legacy ecosystem/status remaps + `Action.direction` column), verified clean. Discovered during impl: company `AWAITING_RESPONSE` status existed only client-side — added to eliminate set. Turso table names are PascalCase model names (`"Contact"`). |
| 2026-06-12 | **Phase 2 touch-ups built + deployed** (third session, commits `e099388`…`d718ffa`). Edit/delete on `/meetings` (Quick Log dialog → canonical editor with edit mode); meeting prep notes (`ConversationPrepNote`); attachments (`ConversationAttachment` + `POST /api/upload/file`); `MarkdownTextarea` speed-typing component; backup paths → 26 tables, version 4. User ran the 2 additive CREATE TABLEs in the Turso console. One Vercel build failure (unused var caught by `tsc -b` but not `tsc --noEmit` — client build is stricter than typecheck) fixed in `d718ffa`. **Search upgrade plan written** — next build target. |
| 2026-06-13 | **Planning session — UX/Search/Meetings plan approved, no code.** Owner brought a 13-item worklist; produced + got sign-off on `UX-SEARCH-MEETINGS-PLAN.md` (Phases A–E: quick wins, search fixes/perf, Quick Log unification, meetings calendar, terminology). 3 decisions locked (unify on Quick Log; LinkedIn paste-only; separate meetings calendar). Diagnosis: 20s search = Turso query count not data volume (lazy-related + parallelize, no FTS); no schema changes needed. |
| 2026-06-13 | **Phase A of UX/Search/Meetings plan built + deployed** (commits `fe0c457` A1, `dcf2adc` A2+A3, `8f1e1f9` A4, `72aa6bf` docs, `ab3efea` A2 follow-up fix). A1 Consultant ecosystem (client-only — `ecosystem` is a plain passthrough TEXT column with no validation); A2 clickable top-bar Search button; A3 one-tap clear on Search + Meetings search; A4 markdown format-before-typing. **No schema changes.** A2 button rewired to `navigate('/search')` to match **Ctrl+K**. Verified desktop + 390px. |
| 2026-06-14 | **Task 5 — long-lived PrismaClient** (`7551da2`); Actions/Ideas Polish batch complete (all 5 shipped). Retired the per-request `resetPrisma()` middleware (the 2026-03-24 stale-connection fix); `db.ts` keeps one client and rebuilds it only on an actual connection error, retrying the failed query once. Schema-free. Verified locally and on the live Turso-on-Vercel deploy across two idle cycles (6 min + 9 min). |
| 2026-06-14 | **Favorite organizations** (`80911ff`). Mirror of favorite contacts, schema-free: reserved `Favorite` `CompanyTag` + `GET /companies/favorites` / `PATCH /companies/:id/favorite`. Star toggle + amber quick-add chips in Quick Log "Organizations" and Ideas "Related Companies". |
| 2026-06-14 | **Calendar polish** (`85ab6ec`). Meetings calendar: `dayMaxEvents={false}` so a day shows ALL its meetings inline. Hover tooltip via `eventDidMount` `el.title` = first participant + summary. |
| 2026-06-14 | **Backup binary coverage fixed** (`c5c18b5`). Manual ZIP now bundles ALL binaries: `photo-backup.ts` → `collectBinaryRefs`/`buildBinariesZip` adds `ConversationAttachment` files + markdown-embedded `![](url)` screenshots; download renamed `searchbook-photos.zip` → `searchbook-files.zip`. All 27 models confirmed in both backup paths. Matrix: `BACKUP-COVERAGE-AUDIT.md`. Blob CORS from browser CONFIRMED working. |
| 2026-06-14 | **Restore-test harness** (`7d67dc5`). `server/scripts/restore-test.mjs` — schema bootstrap + FK-ordered restore + verify counts/relationships/binaries; prod-safe (`--confirm`, `--forbid-url`). Runbook: `RESTORE-TEST-RUNBOOK.md`. |
| 2026-06-15 | **LinkedIn extraction explanation + Action logic polish (Session 3).** Fixed two Action bugs: (1) Meeting hyperlink in Action Details showed "Meeting #ID" instead of the parsed `conversationDisplayName`; (2) Action Quick Log "contact leak": `actionBody` inadvertently added all meeting `participantIds` as `contactIds` to every action logged from a meeting. Pushed to `main`. |
| 2026-06-15 | **Contacts & Actions UI Polish (Session 4).** Removed legacy fields ("Referred By", "Research") from the contact form while preserving data. Detail view reordered ("How Connected"/"Personal Details"; Actions card above Links). Action creation shows the contact as a neutral quick-add chip for the owner field while defaulting owner to "Me". No schema changes. |
| 2026-06-15 | **Meetings overhaul (Session 5).** (1) Real `Series` entity — `Series` table + `Conversation.seriesId` (`onDelete: SetNull`), revising D4; new `/api/series` CRUD; Series picker in Quick Log; migration auto-grouped existing titles shared by ≥2 meetings. (2) `Conversation.updatedAt` + `/api/meetings` `sortBy`/`sortDir` + Sort dropdown. (3) Card title precedence `title → first participant → contact → company → desc`. (4) Quick Log redesign — promote the big 3 (Participants/Notes/Actions); secondary fields into 3 labeled disclosures. **Turso DDL applied by owner via web console before push.** |
| 2026-06-15 | **Meetings overhaul — follow-ups (Session 5b).** (1) `ConversationParticipant.ordering` so an untitled meeting's card title is the first participant entered. (2) Default Meetings sort = recently updated. (3) Series rename/delete affordances on the series-view header (delete keeps the meetings, `seriesId`→NULL). Second Turso DDL (`ordering` column + rowid backfill) applied by owner before push. |
| 2026-06-15 | **Four owner asks across Ideas/Contacts/Meetings, all schema-free, shipped** (`5314aad`, `dacdeaa`, `4ea562c`, `7f450c6`). (1) Ideas collapsed cards hide images + clamp + "N screenshots" hint. (2) Connecting to a contact promotes their current employer to company status CONNECTED (guard NONE/RESEARCHING-only; helper `server/src/company-status.ts`). (3) Quick Log: Summary behind a caret; prep notes autosave + inline-editable; mobile full-width form (`useIsDesktop`). (4) Prep notes render formatted markdown with pencil↔textarea toggle; fixed the prep/form divider drag closing the dialog (Radix `onInteractOutside` guard). Verified desktop + 390px. |
| 2026-06-15 (s2) | **Ideas & Meetings polish — 9 owner asks** (`IDEAS-MEETINGS-POLISH-PLAN.md`). Idea tags → share app-wide Tag vocab (schema); meeting title → Edit dialog + keep series chip. Tasks 1–7 shipped (`f0c5f37`): Ideas card trim; Ideas Card/List toggle; Ideas description `<mark>` highlight via new rehype plugin `highlight-markdown.ts`; Meetings Next Steps → MarkdownTextarea; Quick Log actions rework (composer rows autosave as real Actions; per-row "Who owns it"); meeting title→Edit + "series" chip. Task 8 — Idea tags → app-wide Tag (`c3f18dd`, SCHEMA): owner applied Turso `CREATE TABLE "IdeaTag"` + backfill. Follow-up `bd1fbb7`: `GET /tags` excludes the reserved `Favorite` tag. Remote `main` = `8fa51cc`. |
| 2026-06-16 | **Contacts Global Sorting & Form Layout (Session 6).** Sidebar reordered. Contacts list upgraded to true global server-side sorting for all columns (`manualSorting: true` + `prismaOrderBy` on `/api/contacts`). Other lists verified to already sort globally client-side. Contact Edit layout reorganized (Companies/Past Roles below Photo, Location below Emails, new "Classification" section for Ecosystem/Status). |
| 2026-06-16 | **LinkedIn Import Bugfix (Session 6).** Past roles extracted via LinkedIn import were missing from the editor's Companies section; import now adds both current and past roles to `companyEntries` (past roles `isCurrent: false`). Silent cleanup strips dummy "Unknown" companies from prior bad extractions. |
| 2026-06-16 | **Data Cleanup Scripts (Session 7).** Created one-off scripts `sweep-company-status.js` (promote Company → CONNECTED if a CONNECTED Contact currently works there) and `delete-researching-recruiters.js`. Executed against local dev DB. |
| 2026-06-16 | **Data Cleanup Execution & Organization Contact Inline Edit (Session 9).** Owner executed `delete-researching-recruiters.js` and `sweep-company-status.js` against **production**. The sweep script was updated to safely convert only `NONE` (blank) statuses to `CONNECTED`, protecting existing statuses. Ecosystem/status badges on an Organization's detail page (`company-detail.tsx`) made interactive (inline dropdown updates). Pushed to `main`. |
| 2026-06-16 | **LinkedIn Import Bugfix (Session 8).** Fixed LinkedIn import defaulting all new companies to `RESEARCHING`. Client now creates the company as `CONNECTED` only if the imported contact currently works there (`isCurrent: true`) AND is `CONNECTED`; otherwise `NONE` (blank). Backend already propagates `CONNECTED` to current employers when a contact becomes connected. |
| 2026-06-16 | **UI polish + meetings-search follow-ups (Session 13).** Seven owner asks, all schema-free, verified live via chrome-devtools, pushed to `main` (`3c12321`). (1) Contact photo box accepts clipboard paste (`photo-upload.tsx`). (2) Contact Notes → `MarkdownTextarea` (`contact-form.tsx`). (3) Quick Log prep-note bar opens when the "Tags, prep notes & attachments" caret is expanded on desktop (`quick-log-dialog.tsx`). (4) Global Search defaults all scopes every visit + "All" reset chip + click-to-isolate/double-click-to-invert (`search.tsx`). (5) Discoverable "Manage series" dialog beside the Meetings Series filter (`meetings.tsx`). (6) New-org status defaults to blank (`company-form.tsx`). (7) Meetings `?q=` now matches title + participant + series name for all meetings + card highlights the hit in heading/series chip/participant badges (`meetings.ts`). |
| 2026-06-18 | **Outlook import — owner UI follow-ups.** (1) Added a **"Tomorrow"** range preset to the Import dialog (between Today / This week). (2) Fixed an iOS-Safari "doubled button" artifact: the right-anchored "Import N" button's left edge moved as the count label changed, leaving a ghost paint after select / select-none — fixed with a stable `min-w-[7.5rem]` (fits up to "Import 188") so the edge never moves. Verified the preset (sets tomorrow's date) + width clamp via chrome-devtools at 390px; `prepush` + `vite build` green. |
| 2026-06-17 | **Outlook → SearchBook meeting import (Phase 5 Task 5.0, commit `bb49185`).** Fail-fast first: built a zero-dep ICS probe, the owner published their NCQA calendar, the probe proved subject/date/time/recurrence come through but **attendees are stripped** (D7). Owner picked Option A (skeleton import). Then built + shipped: schema `Conversation.calendarUid` + `startTime` (Turso DDL applied by owner before push); `server/src/lib/ics.ts` provider; `server/src/routes/calendar.ts` (`GET /events`, skip-only idempotent `POST /import`); `ical-expander` dep; "Import from Outlook" dialog on `/meetings`; time shown on cards + editable in Quick Log. Verified end-to-end against the live feed via chrome-devtools (847 events; found+fixed an ical-expander master/occurrence dup; import/idempotency/no-clobber proven; UI screenshotted); all test data + undo snapshots cleared from the local dev DB. `prepush` + full `vite build` green. **[USER ACTION] remaining:** set `OUTLOOK_CALENDAR_ICS_URL` in Vercel for prod. |
| 2026-06-18 | **Action cards show the person you're waiting on (bugfix, `b1f6bc5`, schema-free).** When an action is owed by someone else (`WAITING_ON_THEM`), compact action displays showed the "Related To" contact, not the ower (e.g. related to Kara but owed by Scott → showed "Kara"). Root cause: the API returned only ower **ids**, never names. Fix: server `attachOwers()` resolves `owerContactIds` (a JSON column, not a relation) → `[{id,name}]` in one batched query, attached as `owers` on every action read/write response; client `actionDisplayPeople()` helper shows owers when present, else related contact(s) — wired into the dashboard `ActionRow` + the Actions list "Contact" column (incl. sort + global search) with a fuchsia "waiting on" hourglass cue; the calendar already handled owers. Action detail now surfaces a "Waiting on" field beside "Related To". Verified all surfaces + the owed-by-me inverse via chrome-devtools (desktop + 390px); test action + undo snapshot cleared from local dev DB; `prepush` + `vite build` green. Pushed to `main`. |
| 2026-06-16 | **Repo cleanup & session-doc reorg (housekeeping, no app changes).** Commits `191191c` (chore) + `8539666` (docs), pushed to `main`. Moved ~13 applied migrations + orphaned server-root one-offs to `server/scripts/archive/` (kept the 5 reusable tools top-level); deleted `server/repro_output.json`; archived the 10 shipped/superseded `*-PLAN.md` docs to `.planning/archive/` + added `.planning/README.md` index. Single-sourced the session protocol in `AGENTS.md` (CLAUDE.md + `Gemini_session_*.md` → pointers). Split the decisions ledger: full ledger + session log now here in SESSION-HISTORY.md, `STATE.md` keeps only the in-force subset. Rewrote stub `README.md`; refreshed `docs/architecture.md` + `docs/scriptReferences.md`. Renamed the NCQA plan's "Decisions needed" section → **"⏳ Waiting on owner (blocks Phase 3+)"**. `prepush` + server build green. |
| 2026-06-18 (s2) | **Enhancements session — 4 owner asks.** Three shipped (schema-free): (1) **LinkedIn title preference** (`6923f4f`) — contact Title now prefers the current Experience role title over the LinkedIn headline (headline kept as `parsed.headline` backup); server post-processing in `routes/linkedin.ts` + preview "Headline (backup)" line. (2) **Participant hover tooltips** (`aa7a8e5`) — hover a participant on a meeting card or in the editor → title + current employer via new shared `PersonTooltip`; `/meetings` participant include + `/contacts/names` now carry `title` + primary `company.name`. (3) **Doc** (`b1826f9`) — marked the `OUTLOOK_CALENDAR_ICS_URL` [USER ACTION] done (owner set it in Vercel). (4) **@-mentions in meeting notes** (`e006895`, SCHEMA — **committed locally, push held for Turso DDL**): type `@` in notes/next-steps to flag a third person inline (existing contact or a "loose" name not yet a contact); review on new `/mentions` page + "Mentioned in Meetings" contact card; loose mentions → one-click "Create contact" (rewrites the note token). New `ConversationMention` table (contactId nullable), derived from note tokens on every conversation save; `@` autocomplete is opt-in on `MarkdownTextarea`; chips render via `MentionableMarkdown`; backup both paths + version 6. Verified end-to-end via chrome-devtools (capture → sync → Mentions page → create-contact → contact card → meeting card); test meeting + stub contact cleaned from local dev DB. `prepush` + full `vite build` green. **[USER ACTION]:** run `CREATE TABLE "ConversationMention"` + 2 indexes on Turso, then push `e006895` + the docs commit. |
| 2026-06-18 (s2 cont.) | **@-mentions shipped + tooltip follow-up.** Owner ran the `ConversationMention` Turso DDL → pushed `e006895` + docs (`58a5d1b`), @-mentions live. Owner flagged the participant tooltip didn't fire over the editor's **Participants combobox pills** (worked on card badges + rows). Fix `99f052a`: `MultiCombobox` gains optional `optionMeta` map → wraps each selected pill in `PersonTooltip`; meeting editor passes `contactMeta`. Verified the pill tooltip via chrome-devtools (Susan L. Glover pill → her title). Remote `main` tip = `99f052a`. |
| 2026-06-18 (s3) | **@-mentions enhancements — 4 owner asks (3 live, 1 held for DDL).** (1) **Surrounding-context snippets** (`d6e39a6`, live): `/mentions` page + contact "Mentioned in Meetings" card now show the text *around* the mention (`mentionSnippet` ±140 chars, snapped to word/token boundaries, protected-range aware so it never cuts a `[…](…)` token; chip preserved), not the whole note. (2) **@-mentions in prep notes** (`2b8d0b8`, live, schema-free): prep-note editors open the `@` picker + render chips; new `resyncConversationMentions()` re-derives the index from notes+next-steps+**all prep notes** (called from conversation create/update AND prep-note CRUD); `create-contact` now rewrites loose→bound tokens in prep notes too; `/mentions` returns prep-note content for snippets. (3) **Org @-mentions — full parity** (`cd2bfdc`, **SCHEMA, committed, push held for DDL**): `@` picker offers orgs (building icon) + "new organization" loose option; violet chips → `/companies/:id`; `/mentions` org badges + one-click "Create organization" (`POST /mentions/:id/create-company`); new "Mentioned in Meetings" card on org pages; `?companyId` filter. `ConversationMention` + `companyId` (FK→Company SetNull) + index + `kind` ('CONTACT'\|'COMPANY', disambiguates loose); tokens `[@Org](/companies/ID)` / `[@Org](#org-mention)`. Also merged Mentions-page snippets per field (clustered mentions → one block, no N near-dupes). (4) **Note-image lightbox** (`7b7dcad`, live, schema-free): one app-root `NoteImageLightbox` delegates capture-phase clicks on any `.prep-note-markdown img` → full-screen overlay (fit↔actual-size, Esc/backdrop, body-scroll-lock); fixes unreadable small pasted screenshots; `zoom-in` cursor. Verified all 4 end-to-end via chrome-devtools on local dev (DB migrated locally: 2 `ADD COLUMN` + index; all 4 mention kinds parsed; create-company rebind; org card; `@` picker shows orgs+both loose; lightbox opens 85%-dark full-screen); test meeting+company cleaned up. `prepush` + full `vite build` green. Pushed Tasks 1/2/4 (remote tip `7b7dcad`); `cd2bfdc` held. **[USER ACTION]:** run the 2 `ADD COLUMN` + index DDL on Turso, then `git push origin main`. |
| 2026-06-19 | **Ideas autosave + second-level bullets (2 owner asks, schema-free, pushed).** (1) **New-idea autosave — meeting-log parity:** replaced the `useAutoSave` hook in `idea-list.tsx` with the Quick-Log bespoke serialized save chain (`enqueueSave` + `savedIdRef` + snapshot dedup). Typing a title POSTs the idea; later edits PUT; free-text contacts/companies resolve only on finalize ("Done") while tags still create eagerly during autosave. Footer mirrors meetings (Delete this idea / Close / Done + Revert-when-dirty); Saved/Saving indicator now shows in create mode too. Existing-id detection switched from `allContacts` membership to the `/^\d+$/` numeric test (matches meetings; removes a load-order data-loss risk). (2) **Second-level bullets (Tab to nest):** shared `MarkdownTextarea` indents/outdents list items with Tab / Shift+Tab (2 spaces/level → CommonMark sub-lists; no-op off a list line so Tab still moves focus); Enter on an empty *nested* item outdents one level (Word-style) before ending the list; new `index.css` nested-list markers (disc → circle → square; ordered → lower-alpha → lower-roman). Applies to Ideas, contact notes, prep notes, and meeting notes/next-steps. Verified end-to-end via chrome-devtools (desktop + 390px); test idea cleaned from local dev DB; `prepush` + full `vite build` green. Pushed to `main`. |
| 2026-06-19 (s2) | **One-click clear on list-filter search boxes (1 owner ask, schema-free, pushed `2375820`).** Owner asked that every standalone search box (magnifying glass on the left) afford clearing in one click. Replicated the existing clear-button pattern (already on global Search `search.tsx` + Meetings title filter `meetings.tsx`) onto the four that lacked it: Ideas, Contacts, Companies, Actions list pages. Each renders `{value && <button onClick={()=>setValue('')} aria-label="Clear search" className="absolute right-0 top-0 … h-9 w-9"><X/></button>}` after the `Input`, plus a conditional `pr-9` so text doesn't slide under the X (`X` added to lucide imports in idea-list + action-list; the other two already imported it). Combobox/command-palette typeahead inputs ("Search or type new name…") left as-is — different popover UX, not "search boxes like this one." Verified on Ideas via chrome-devtools (desktop + 390px): typing reveals the X, clicking empties + restores the full list. `prepush` + full `vite build` green. |
| 2026-06-19 (s4) | **Three owner asks — tags in Global Search, clearable meeting time, inline meeting date (all schema-free, verified live, pushed).** (1) **Tags first-class in Global Search** (`02d1a56` + fix `d41a44c`): `/search` accepts `tagIds` (a tag filter alone searches — no text needed; multiple tags OR; ANDed onto any text query), every people/org/meeting/idea result returns its `tags` rendered as clickable violet chips that add to the filter, and a "Tags" `MultiCombobox` fed by `/tags` is the catalog of available tags (reflected in URL `?tags=`). Actions (untagged) excluded under a tag filter. Idea search switched from the legacy comma-string to the `IdeaTag` junction (legacy string still matched). Follow-up fix: reserved `Favorite` tag excluded from result chips + evidence (mirrors `/tags`) — caught in browser verification (NCQA org showed a stray "Favorite" chip). (2) **Clearable meeting start time** (`fcdaac0`): explicit "×" beside the `type="time"` input in Quick Log (native clear is hidden/absent on mobile); empty → `startTime: null` (route already persisted null). (3) **Inline meeting date** (`fce6b93`): each `/meetings` card's date is a popover editor (Today / Yesterday / custom) — `MeetingDateSelect`, partial `PUT {date, datePrecision:'DAY'}`, mirrors inline contact-status/action-due-date; picking a concrete day normalizes precision to DAY; trigger stops propagation so it never toggles card expand. Verified all three end-to-end via chrome-devtools (desktop + 390px): tag-only search returns a tagged contact with a clickable chip + no min-char prompt; clear-time shows/blanks/hides; inline date 2025-12-01/MONTH → today/DAY then restored. All test mutations (tag add, date change) reverted on the local dev DB. `prepush` + full `vite build` + server typecheck green. **Post-ship:** owner reported a never-stopping browser busy-spinner, suspected a Turso hang from the new search query; investigated and **ruled it out** (local dev clean — `readyState: complete`, no pending/looping requests, SW active in dev too) — owner isolated it to a **local VS Code / harness artifact** (spinner tracks with VS Code open and persists with the browser fully closed), not the app. No code change. |
| 2026-06-21 | **De-duplicating CSV import — match existing contacts by name + merge emails (owner ask, schema-free, pushed `6fb67e7`).** Owner wanted to bulk-import a list of NCQA email addresses without creating duplicates of contacts already in the DB. Before: "Import Contacts from CSV" always `POST /contacts` per row → guaranteed duplicates (and the list endpoint's `select` doesn't even return `email`, so client-side matching wasn't viable). Built an opt-in **"Update existing contacts (match by name)"** toggle backed by a new server endpoint **`POST /api/contacts/import-match`**: case-insensitive name match → exactly one match **merges the email only** (primary if empty, else appended to `additionalEmails`, deduped) and **never clobbers** ecosystem/status/any other field; no match → create new (default ecosystem `NETWORK`, per owner); >1 match → ambiguous, skipped + reported. `dryRun` classifies every row without writing, powering a preview card (update / create / no-change counts + ambiguous names) before commit; the legacy create-only path is untouched when the toggle is off. Verified end-to-end: server via direct API assertions (merge-to-additional, set-primary, create NETWORK/CONNECTED, ambiguous skip, already-on-file no-op, no duplicate, no clobber — test rows cleaned up); full browser flow against the real 60-row `ncqa_email_addresses.csv` (auto-mapped Name+Email, dry-run = 3 matched existing [Madeline Henry / Julie Seibert / Tricia Elliott] · 57 new · 0 clobbered) — read-only, closed without applying so no dev-DB writes. `prepush` + full client+server build green. Pushed to `main`. |
| 2026-06-19 (s3) | **Bugfix: false "changed on another device" 409 blocking edits (owner ask, schema-free, pushed `5910384`).** Owner couldn't edit an Organization (CMS) — every save fired the conflict toast + reverted, with no concurrent editing. Root cause (diagnosed by reproducing the guard against the real libsql adapter on a copy of dev.db): the optimistic-concurrency guard filtered `where: { id, updatedAt: <Date> }`; Prisma 7 stores DateTime as text `...+00:00` and binds that same form in equality filters, but backup-restore/bulk-import/raw-SQL writes store `...Z` (or `YYYY-MM-DD HH:MM:SS`) → the filter matched 0 rows even at the identical instant → permanent bogus 409 on every save of any record last written outside Prisma's typed path (a normal edit "heals" the row). Fix: replaced the DB-level filter in the contact/company/action PUT routes with an app-code epoch-ms comparison (`assertNotStale` in `concurrency.ts`) against the already-fetched row — representation-independent; genuine cross-device saves still 409. Verified on the libsql adapter (real `...Z` row) + end-to-end over HTTP (correct token → 200 (was 409); wrong token → 409). Recorded the Prisma datetime-equality gotcha in CLAUDE.md. `prepush` + full client+server build green. |
| 2026-06-22 | **CSV import "Reports To" relationships (owner ask, schema-free, pushed).** Owner had a 2-column org-chart CSV (`Name`, `Reports To (1-up)`) and wanted the enrich import to turn the manager column into a `REPORTS_TO` relationship. Extended the existing **`POST /api/contacts/import-match`**: each row may now carry a **`reportsTo`** (manager name) + the call carries a **`defaultEcosystem`**. After each subject is matched/created, the manager is resolved via the same case-insensitive name index (match existing, else create a bare contact, indexed so repeats de-dup) and a **`REPORTS_TO` relationship** is created `subject → manager` (matches the `[from] reports to [to]` UI direction). **Idempotent** (existing/just-created pairs never duplicated — a pre-load of all `REPORTS_TO` pairs + a per-run set); blank / **"Not found"** / self-reference → no relationship; ambiguous manager (name matches >1 existing) → skipped + reported. `dryRun` predicts everything via negative **synthetic ids** (one code path, no writes) so the preview shows relationships-to-create + new-manager names. Client (`csv-import-dialog.tsx`): new **"Reports To (manager)"** field mapping + header aliases (incl. `reports to (1-up)`); mapping a Reports-To column **auto-routes through the match endpoint** (so subjects + managers de-dup); new **"Ecosystem for new contacts"** picker (default General Network); preview adds a relationship/new-manager summary; apply toast + button label include relationship counts. Verified end-to-end: server via a throwaway script (dry-run on the real 60-row `qmrg_1up_reporting.csv` = **57 relationships** [= 60 − 3 "Not found"], 14 managers created, 0 ambiguous, 0 errors; real-write test asserted direction + ecosystem + idempotent re-run + not-found/self skips, then cleaned up its 2 contacts + relationship); full browser flow via chrome-devtools (desktop + 390px) — auto-mapped, ecosystem→NCQA Internal, preview "57 rel / 14 new managers" incl. surfacing name-form near-dups ("Josie Granner" vs "Josephine (Josie) Granner", "Keirsha Thompson" vs "Keirsha (KEER-shuh) Tompson"); closed without applying so the dev DB was untouched. `prepush` + full client build green. **Schema-free** (`Relationship`/`REPORTS_TO` already existed → no Turso DDL). Pushed to `main`. **Also (planning):** the owner's "does this look right?" review of a production dry-run exposed that the enrich import only ever touches **email** on matched contacts (the no-clobber rule) and flagged near-dup managers (e.g. "Vivek Garg" vs existing "Vivek (Viv-ACHE) Garg") — the owner then **normalized those manager names in the CSV** so they resolve to the existing contacts (no spurious new-manager dupes). Agreed next-session task = **any-field enrich import**, with the pivotal policy decided now via AskUserQuestion: **fill blanks only** (write a CSV value into a matched contact's field only when it's currently empty; never overwrite; no per-column toggle / no global overwrite for v1). Full design + sub-decisions in `NEXT-SESSION-PROMPT.md` "What's Next" #1. |
| 2026-06-22 (s2) | **Any-field enrich import — "fill blanks only" (owner ask, schema-free, pushed `0ac5c0b`).** Implemented last session's agreed primary task: the name-match path on **`POST /api/contacts/import-match`** previously only merged **email** into a matched contact; it now **ENRICHES "fill blanks only"** — any mapped scalar field that is **currently empty** on the contact is filled from the CSV, while **non-empty (curated) fields are NEVER overwritten** (per the owner's AskUserQuestion decision: fill-blanks-only, no per-column toggles, no global-overwrite for v1). Server (`server/src/routes/contacts.ts`): new `buildFillBlanksPatch` over the scalar set (`title, roleDescription, phone, linkedinUrl, location, howConnected, mutualConnections, whereFound, openQuestions, notes, personalDetails`); email keeps its existing additive merge; **company** fills `companyId` only when the contact has **no current employer** (via `currentEmployerCompanyIds` + `resolveCompany`; promotes that company to CONNECTED when the contact is CONNECTED — same rule as create; no 2nd-employer append in v1); **`ecosystem`/`status` excluded entirely**. The contact index `findMany` now selects the fill fields (incl. notes/personalDetails — acceptable for a one-shot single-user import, still no `_count`). `dryRun` predicts everything and the result reports new **`fieldsFilled`** + **`fieldsFilledByName`** (+ per-row `filled` count). Client (`csv-import-dialog.tsx`): relabel "Add email to existing" → **"Enrich existing (fill blanks)"**; new **blank-fields-filled breakdown** ("N blank fields will be filled across M contacts" + "Title ×2, Phone ×1, …"); updated checkbox/footer copy, completion summary, toast ("Enriched N, … X blank fields filled"), and apply-button label ("… enrich, … new"). Verified end-to-end: two throwaway HTTP scripts (fill-a-blank + leave-non-blank-untouched + email-as-primary + ecosystem/status untouched + company-fill + idempotent re-run = 0 changes + additive 2nd email; plus a company-guard proving company is NOT filled when an employer already exists), all assertions green, test rows + companies cleaned up; full browser flow via chrome-devtools (desktop + true-390px device emulation) on a CSV matching two seeded contacts + one new name → preview "2 enrich / 1 new / 0 skip", "5 blank fields across 2 contacts · Title ×2, Phone ×1, Location ×1, Notes ×1", "2 reporting relationships" — closed without applying, seeded test contacts deleted, dev DB clean; console clean (only a benign PWA meta-tag deprecation). `prepush` + full client `vite build` green. **Schema-free** (same endpoint, additive logic → no Turso DDL). Pushed to `main`. **Follow-up (same session, `ad3b529`):** beefed up the CSV-import **column auto-mapper** to recognize many more reasonable header synonyms — added `normalizeHeader` (case/punctuation/separator-insensitive), greatly expanded `HEADER_ALIASES` for every field incl. the ones that previously had none (`roleDescription, howConnected, mutualConnections, whereFound, openQuestions, personalDetails`; e.g. mutualConnections now catches "mutual connections" / "connections" / "connecting people" / "mutual"), and replaced the naive `includes()` fallback with `autoMapField` (alias → exact → a conservative single-candidate fuzzy fallback that leaves ambiguous headers like "Company Name" unmapped rather than mis-assigning; "name" excluded from loose single-word matching); also auto-map no longer maps two columns to the same field. Verified via chrome-devtools on a 19-column CSV of alternative headers (all mapped correctly). `prepush` + full client build green; client-only, schema-free. |
| 2026-06-23 | **Preferred name / pronunciation field (owner ask, SCHEMA, committed `e8fa48e`, push held for Turso DDL).** Owner stored "what to call / how to say a name" inside `Contact.name` (`Benjamin (Ben) Glicksberg`, `Vivek (Viv-ACHE) Garg`) — great to read, but it broke joins against email lists carrying the formal name (and had already spawned a duplicate "Ben Glicksberg" #206 alongside #208). Added additive **`Contact.preferredName`** ("Goes by / pronunciation") that holds the spoken form **apart from `name`**, keeping `name` clean for CSV joins/imports/exports. UI recombines as **"name (preferredName)"** at display time via new `contactDisplayName()` helper — verified rendering on the **contact list**, **detail header**, **global search**, and **command palette**; editable on the contact form (full-width field under Name, with live `"Name (Spoken)"` preview helper text). Searchable server-side in **both** the contacts-list search (`/contacts?search=`) and global search (match reason "goes by"). One-shot back-fill `server/scripts/migrate-contact-preferred-name.js` (dual-mode local/Turso, **dry-run by default**, idempotent, never overwrites an existing `preferredName`) splits the first parenthetical out of `First (Spoken) Last`; **local dev DB migrated — all 22 legacy names converted, 0 names still contain "("** (incl. tricky `Stephen (Steve) J. Watt`→`Stephen J. Watt`, `M Kennedy (Kennedy) Hall`→`M Kennedy Hall`). Verified end-to-end via chrome-devtools (desktop + 390px): list shows "Benjamin Glicksberg (Ben)", detail h1 recombines, edit form has clean Name + "Goes by / pronunciation"="Ben"; server round-trip — GET returns it, PUT persists, search "Viv-ACHE" finds Vivek (reason "goes by"). `prepush` + full client `vite build` green. Backup/restore both paths flow the new column automatically (full-row read/write — no code change). v1 display scope = primary contact surfaces only (participant chips / action references / pickers still show bare `name`). **[USER ACTION] before push:** on Turso run `ALTER TABLE "Contact" ADD COLUMN "preferredName" TEXT;` then the back-fill (web SQL console SELECT-preview → UPDATE, or the script with a fresh token), then `git push origin main`. Also surfaced the #206/#208 "Ben Glicksberg" duplicate for the owner to merge. |
| 2026-06-23 (s2) | **Pronunciation in meeting person-hover tooltips (owner ask, schema-free, pushed `d4f9c40`).** Follow-up to the preferred-name field: in the meeting flow, every place you hover a person now shows their pronunciation/nickname (`preferredName`) in the tooltip — the Participants **combobox pills**, the editor's **participant rows**, and the **`/meetings` card badges**. `PersonTooltip` gained a `pronunciation` prop rendered as **"🗣 Viv-ACHE"** in the header slot (takes precedence over `name`, which isn't repeated since it's already on the hovered chip), above the `title · employer` line. Data threaded: `/contacts/names` + the `/meetings` participant include now `select` `preferredName`; the quick-log `contactMeta` (passed as the combobox `optionMeta`) and the shared `Conversation.participants[].contact` type carry it. Verified end-to-end via chrome-devtools: added Vivek Garg to a Quick Log meeting → both the combobox pill **and** the participant-row tooltip rendered "🗣 Viv-ACHE / President & CEO, NCQA · NCQA" (no redundant full name); throwaway autosaved meeting deleted (count 224→225→224). `prepush` + server/client typecheck green. **Schema-free** (the `preferredName` column already exists in prod) → pushed straight to `main`. Mobile not re-tested — hover tooltips are a desktop affordance with no touch rendering; dialog layout unchanged. |
| 2026-06-24 (s2) | **Contact-merge data-loss + meeting-dialog autosave/lookup fixes (owner bug report, schema-free, pushed `95cd537` + `8cbc7ee`).** Owner hit a cascade of surprises after merging two just-created "Seth Glickman" contacts: the merged Seth vanished from a meeting he'd attended, the meeting's title (which falls back to its first participant) went blank, and he wasn't selectable to re-add as a participant even though he was visibly in Contacts; separately, an earlier write-up dismissed via the dialog "×" had silently not saved. Diagnosed three distinct bugs. **(1) Merge silently destroyed data** (`fix(merge)` `95cd537`, `server/src/routes/duplicates.ts`): the contact merge re-pointed the anchor contact/actions/links/prep-notes/relationships/employment but **never handled three Contact relations**, so deleting the duplicate lost data via their onDelete behavior — `ConversationParticipant` (Cascade → a meeting whose only participant was the removed contact lost that participant *and* its fallback title), `ActionContact` (Cascade → multi-select action ownership dropped; only the legacy single `Action.contactId` was migrated), and `ConversationMention` (SetNull → mentions orphaned to a null id). Now re-points all three to the kept contact before the delete, with composite-PK dedupe (carrying a per-person takeaway note onto the kept participant row when it had none); for mentions it rewrites the `(/contacts/<id>)` link target in the note text (notes/next-steps/prep-notes) **via raw SQL** so the re-derive-on-save sticks *and* it doesn't bump `Conversation.updatedAt` (matches the existing anchor-move philosophy — a merge must not float old meetings up "Recently updated"), then rebuilds the mention index for each affected meeting. **(2) Dialog close discarded unsaved work** (`fix(meetings)` `8cbc7ee`, `client/src/components/quick-log-dialog.tsx`): closing via ×/Esc/Cancel/click-outside never flushed the pending ~1.5s debounced autosave (the timer was canceled on close), and free-text (brand-new) names are deliberately excluded from the autosave body — so a new participant + notes typed and dismissed before autosave fired was lost. Refactored `handleSave` into a shared `finalizeMeeting({ silent })` and added `hasUnsavedWork()`; closing now flushes any unsaved work (resolving free-text names like an explicit Done), holding the dialog open until it resolves — closing is "keep my work", not a discard (the Delete button is the discard). **(3) Stale lookups** (same commit): the contact/company/tag lists were fetched once per session and cached (the dialog is mounted permanently at app root via `QuickLogProvider`), so a contact created or merged mid-session was invisible to the participant/org pickers and the `@`-mention autocomplete until a full reload — removed the `lookupsLoaded` gate so they refetch on every open. Server + client typecheck + full client `vite build` green; rebased onto the parallel reminders session and re-verified post-rebase. **Schema-free** → pushed straight to `main`. **Not done (owner's choice):** the specific pre-fix Seth meeting still needs his participant link re-added (reopen the meeting → add him, now that the picker refreshes); a one-off audit/repair of any *earlier* merges that may have similarly lost participant/actionContact links or orphaned mentions was not run. A `@`-mention false alarm (#4 in the original diagnosis — a loose mention of a third party) turned out to be fine (owner had just had trouble finding it); no fix needed there. |
| 2026-06-24 (s3) | **Selecting an action time-of-day auto-enables "Remind me" (owner ask, schema-free, pushed).** Follow-up to the reminders feature: the owner wanted picking a time to imply they want the alert. Both editing surfaces — the inline `ActionDateSelect` popover (`updateTime`) and the full action form's time `<Input>` — now flip `notify` ON when a non-empty time is set **and** `notify` is currently off (so an explicit later toggle-off isn't fought within the same edit), running the same `ensurePushForReminder()` device-subscribe + "enable notifications in Settings" fallback toast as the manual bell. Reminder can still be toggled back off; clearing the date still drops time+notify (pre-existing payload gating, since notify only persists with a date). Runbook (`ACTION-REMINDERS.md`) note added. `npm run typecheck` (client+server) + full client `vite build` green. Schema-free → pushed to `main`. |
| 2026-06-24 | **Action time-of-day + opt-in Web Push reminders (owner ask, SCHEMA, deployed `9825a3a` + dedicated-secret fix; verified end-to-end in prod).** Owner wanted actions to optionally carry a **time of day** and fire **desktop/PWA notifications**, **without adding cost** and **without disturbing** the dominant "no times, no alerts" pattern. Designed fully opt-in + additive: `Action.dueTime` ("HH:MM" local), `Action.notify` (default false), `Action.lastNotifiedAt`, new `PushSubscription` table — `dueDate` left **date-only** so all existing string comparisons/sorting are untouched; an action with no time and `notify=false` behaves exactly as before. Per AskUserQuestion, **time and alert are independent** (a time can exist with no alert; an alert with no explicit time → **09:00 America/New_York**). Delivery is real **Web Push** at **$0**: free VAPID + a **free external 1-minute cron** (cron-job.org) hitting `GET/POST /api/cron/reminders` (CRON-secret-gated, exempt from the `/api` password gate) — it converts each due action's wall-clock moment in `REMINDER_TZ` to a real instant, fans a push out to every `PushSubscription`, sets `lastNotifiedAt` once, and prunes dead (404/410) subs; editing date/time/notify **re-arms** (`lastNotifiedAt`→null). Server: `lib/push.ts` (VAPID + DST-correct `zonedWallTimeToUtc`), `routes/push.ts` (public-key/subscribe/unsubscribe), `routes/reminders.ts`; `web-push` dep; `dueTime` `HH:MM` validation in the actions routes. Client: time input + "Remind me" bell in `action-date-select.tsx` and the action form; time/bell shown in lists & detail; **Settings → Notifications** card to enable/disable push per device; `lib/push.ts` subscribe flow; `public/push-sw.js` (`push` + `notificationclick`) imported into the Workbox SW via **`importScripts`** (kept `generateSW` — existing `/api` NetworkOnly + `/photos` CacheFirst rules untouched, lower-risk than `injectManifest`). **Dashboard** (owner follow-up): today's **timed** actions sort to the top in time order, and a timed action **shifts into the Overdue card** once its moment passes. Built on branch `claude/actions-time-notifications-i013g7`, `prepush` + full client `vite build` green (fixed a `Uint8Array<ArrayBuffer>` typing the stricter `tsc -b` caught), merged to `main`. **Turso DDL applied by the owner** (3 ADD COLUMN + `PushSubscription` table). **Setup snag resolved:** the existing backup `CRON_SECRET` is marked **Sensitive** in Vercel (unreadable), so it can't be reused in the cron URL → added a dedicated, readable **`REMINDERS_CRON_SECRET`** (`reminders.ts` falls back to `CRON_SECRET`). Also diagnosed two owner-facing red herrings: pasting the cron URL in the normal browser shows the SPA shell (the PWA service worker intercepts the address-bar navigation — test in Incognito) and the early 401s were pre-deploy/pre-env-var (Vercel only injects a new env var into builds made **after** it's added → Redeploy). Owner re-set the secret to letters+numbers only (avoids URL-encoding issues), Incognito test returned `{"ok":true,"due":0,"sent":0}`, enabled notifications on phone (home-screen PWA) + desktop, and **a live reminder fired successfully.** Runbook: `.planning/ACTION-REMINDERS.md`. `PushSubscription` intentionally excluded from the DB backup (device-ephemeral). |
| 2026-06-25 | **Backup coverage audit + fix — `Series` & `IdeaTag` were missing from both backup paths (owner ask, schema-free, pushed `2dcd3b8`).** Owner asked to confirm that automated + manual backups, after the recent additions, still fully restore everything. Audited the 32 Prisma models against both enumerations (server `buildExport`/cron→Blob **and** the browser-direct Turso `TABLES_PARENT_FIRST`, plus the matching `/import` + `importViaTurso` restore orderings): they covered only **28 of 30 user-data tables**. Two tables shipped *after* the list was last touched and were silently omitted — **`Series`** (recurring-meeting series; `Conversation.seriesId → Series.id`; a restore into a fresh DB lost all series names and left conversations with a dangling `seriesId`, which under FK enforcement aborts the whole restore transaction) and **`IdeaTag`** (tags-on-ideas junction; ideas silently lost their non-legacy tag links on restore). Fix (`fix(backup)`): added both to the browser-direct list (`Series` before `Conversation` so inserts are FK-safe and the reverse deletes it *after* conversations; `IdeaTag` after Idea+Tag), to the server export object + `Promise.all`, and to the server `/import` delete+insert ordering; bumped backup `_meta.version` 6→7 in both paths; also added the three booleans added since (`notify`, `owedByMe`, `archived`) to `/import`'s `BOOLEAN_FIELDS` so a browser-export → local-dev import doesn't trip Prisma validation. `PushSubscription` (device keys) + `DeletedSnapshot` (undo stack) confirmed *deliberately* excluded as ephemeral. Verified against the local SQLite (throwaway better-sqlite3 audit script, since removed): all 30 user tables now accounted for, 0 unaccounted-for; `Series`/`IdeaTag` exist and are queryable. **Schema-free** (both tables already exist in Turso → no DDL) → pushed straight to `main`. Client+server typecheck + full client `vite build` green. |
| 2026-06-25 (s2) | **Backup-coverage guard — make "forgot to back up a new model" impossible (owner ask, follow-up to the Series/IdeaTag fix).** Owner asked how to *prevent* future schema additions from being neglected in the backup. Added a build-time guard `server/scripts/check-backup-coverage.mjs` (pure Node, no deps) that parses **four** independent enumerations and asserts they agree: (1) `model X` in `schema.prisma` (source of truth), (2) `prisma.X.findMany()` in the server `buildExport`, (3) `tx.X.createMany()` in the server `/import`, (4) the client `TABLES_PARENT_FIRST`. Any user-content model missing from any path **fails the check** with an actionable message; `PushSubscription` + `DeletedSnapshot` are the only `EXEMPT` entries (adding a new ephemeral model forces one conscious edit to that set). Also guards against vacuous passes (errors if a regex matches 0 — i.e. a refactor renamed the patterns) and stale EXEMPT entries. Wired into **`npm run prepush`** *and* **`build:vercel`** (a deploy is blocked too, not just a manual pre-push step). Proved it both ways: passes clean on the fixed tree (30 tables, 2 exempt); temporarily dropped `IdeaTag` from the client list → failed with `client TABLES_PARENT_FIRST: MISSING IdeaTag` + exit 1, then reverted. Added discoverability pointers (comments at both edit sites, a CLAUDE.md Conventions line, the NEXT-SESSION-PROMPT invariant note). Schema-free, pushed to `main`. |
| 2026-06-29 (s2) | **Duplicate dismissals + auto-merge now persist (owner bug report, SCHEMA, DDL applied by owner, pushed to `main`).** Owner reported that after dismissing spurious duplicate matches, the dismissals came back on a return visit / another device — and asked for two new behaviors: a once-dismissed pair should stay dismissed when it recurs via a fresh import, and "combine ABC-D into ABC" should auto-apply to future imports of either. **Root cause:** dismissals were stored only in browser `localStorage` (`searchbook_dismissed_{contact,company}_dupes`), keyed by row id — so they never synced across devices and never matched a reimport's new ids. **Fix** (`server/src/routes/duplicates.ts`, `client/src/pages/duplicates.tsx`, `schema.prisma`, both backup paths): two new tables — **`DismissedDuplicate`** (`type`, `nameKey1`, `nameKey2`) and **`DuplicateMergeRule`** (`type`, `removedKey`, `keptKey`) — **keyed by normalized name** (`normalizeName().toLowerCase()` / `normalizeCompanyNameForDedupe()`, pair ordered alphabetically) so a decision survives a reimport. `POST /api/duplicates/[companies/]dismiss` persists a dismissal; the merge endpoints record a merge rule (`removedKey→keptKey`, only when keys differ). The duplicates scan (`GET`) now returns `{ pairs, autoMergedCount }`: a pair matching a merge rule **auto-merges** (reusing the extracted `runContactMerge`/`runCompanyMerge` transaction — reimported "ABC-D" silently folds into "ABC"); a pair matching a dismissal is hidden; client shows a toast on auto-merges and posts dismissals/optimistically drops the card. **Self-review caught a real bug before sign-off:** the first cut wrote *both* a merge rule and a dismissal on every merge, and the scan checked dismissals *before* merge rules — so a reimported pair hit the dismissal first and was **hidden instead of merged**, defeating the headline feature. Fixed: **merge rules outrank dismissals** (rule check first), merges no longer write a dismissal but **delete any stale one** for the pair (intent changed "ignore"→"combine"). Also fixed two build breakers from an earlier scripted edit (literal U+200B/U+FEFF/diacritic chars in regex → `\uXXXX` escapes for TS1127; a statement stranded outside its function → TS2304). Backup-coverage guard green (30→**32** tables; both new tables in server export + `/import` + client `TABLES_PARENT_FIRST`). Couldn't run `prepush` typecheck locally (registry unreachable, `ECONNRESET`); relied on the dep-free guard + a dependency-less `tsc` pass confirming zero lexer/scope errors, with Vercel as the real gate. **Owner applied the Turso DDL** (2 new tables). Known design choices surfaced to owner: auto-merge fires lazily on Duplicates-page load (no separate import hook); merge rules are permanent with no review/revoke UI yet. |
| 2026-06-29 (s3) | **Meetings list — time-aware date sort + "Upcoming" indicator + "Hide upcoming" toggle (3 owner asks, schema-free, pushed `131a503` / `bbdaccd` / `070a651`).** All on the `/meetings` list view. (1) **Time-aware Date sort** (`131a503`): sorting by date now breaks ties on `startTime` so same-day meetings order by time of day — server `orderBy` becomes `[{date},{startTime}]` (SQLite/libsql ranks NULL `startTime` as smallest → first when asc / last when desc, i.e. untimed = start-of-day, no `nulls` clause needed); `startTime` is zero-padded "HH:MM" so string ordering is correct. (2) **"Upcoming" indicator** (`131a503`, rule refined in `bbdaccd`): future meetings get a sky left-border accent + a small "Upcoming" pill (dot + label, so it's not color-only / PWA-safe). `isUpcomingMeeting` = a future date, OR today with a `startTime` still ahead of now, OR today & untimed & before **5 PM ET** & nothing written up yet (`summary`/`notes`/`nextSteps` non-empty; **prep notes excluded** as pre-meeting). The "now" snapshot is computed in **America/New_York** via `easternNowParts` (Intl `formatToParts`, DST-aware) since meeting dates/times are stored ET — not the browser zone. (3) **"Hide upcoming" toggle** (`070a651`): a Switch by the sort control (added the missing shadcn `client/src/components/ui/switch.tsx`; the unified `radix-ui` pkg was already installed), persisted in the URL `?hideUpcoming=1` like the other filters, list-view only. Filtering is **server-side** so the paged `total`/`hasMore` stay correct — the client sends its ET `today`+`now`; the server's `notUpcomingClause` is the **exact logical complement** of `isUpcomingMeeting` (verified across all four buckets: future dates, today-timed, today-untimed before/after 5 PM ET, documented vs not), so the toggle hides precisely the flagged set. Server skips the filter if `today`/`now` are missing/malformed (no server-side clock guess). Header controls row made `flex-wrap` for the extra control. Client+server typecheck, `prepush` (backup guard, 32 tables), and full client `vite build` + server `tsc` all green. **Schema-free** → pushed straight to `main`. **Mobile (390px) not visually re-tested** — changes are a border accent, a small pill, and a header Switch (row wraps on narrow screens); no dialog/layout changes. |
| 2026-06-29 | **Meeting-participant UX — 5 owner asks, schema-free, pushed `ce9f306` (+ lockfile chore `c0abed3`).** (1) **Create-on-add:** adding a participant to the Quick Log dialog now creates the Contact *immediately* (`handleParticipantsChange` POSTs `/contacts` for a typed-in free-text name and swaps the free-text value→new id in place, carrying any takeaway note) instead of deferring to "Done" — so it has an id at once. (2) **Click-through:** participant names in the editor are now `<Link>`s to `/contacts/:id` (flush + close the dialog on the way); meetings-list pills already linked. (3) **Contacts default sort** "most-recently-updated on top" was **already correct** (client `updatedAt desc` + server honors it) — verified live (created a contact via API → landed on top → deleted it; net-zero), no code change. (4) **Auto-cleanup:** a contact created via the participant field that's removed again before gaining other info is deleted (`autoCreatedParticipantsRef` per dialog session; `ConversationParticipant` is onDelete Cascade so it's clean; matched contacts never tracked). (5) **Bulk paste:** new `POST /api/contacts/resolve-participants` resolves a pasted Outlook recipient list (`Name <email>; …`) — match by email (primary/`additionalEmails`, case-insensitive) → exact name → else create (CONNECTED/NETWORK, email saved); `MultiCombobox` gained opt-in `onBulkPaste` (intercepts only `;`/newline/`<email>`-shaped pastes); dialog `parsePastedPeople` + `handleBulkPasteParticipants` merge ids deduped, populate tooltip/@-mention metadata, track new ones for auto-cleanup, toast a summary. Verified the endpoint live against the real DB (new-create, in-paste dedup, name-match w/ title+company, case-insensitive email-match ignoring a wrong name) — all created test rows deleted after. Client+server typecheck, full client `vite build`, and `prepush` (backup guard) all green. |
| 2026-06-30 | **Action reminders — weekday/weekend default time + forgiving time entry (2 owner asks, schema-free, pushed `4a42849` to `main`).** Both target the action **Time (optional)** field. (1) **Default reminder time 8 AM weekdays / 10 AM weekends** (was a flat 09:00): new `defaultReminderTime(dueDate)` keyed on the due date's weekday (`getUTCDay()` over the `YYYY-MM-DD` parts — calendar weekday is tz-independent), added to **both** the server (`server/src/lib/push.ts`, replacing the `DEFAULT_REMINDER_TIME` const, consumed by the cron's `reminderDueInstant`) and the client (`client/src/lib/action-time.ts`, driving the "Remind me (defaults to …)" hint, which now reflects the picked date). Changes existing implicit-time reminders (notify on, no `dueTime`) from 9→8/10 — intended. (2) **Forgiving free-text time input** — fixes the owner's screenshot bug where the native `<input type="time">` segmented picker rejected partial entries like "9a" with a "Please enter a valid value" popup. New `client/src/components/time-input.tsx` (`TimeInput`) + `parseTimeInput` in `action-time.ts` replace the native input on **both** action surfaces (the full action form `action-form.tsx` and the inline `ActionDateSelect` popover): a bare hour assumes **:00** and an `a`/`p` suffix sets AM/PM ("9"/"9a"→09:00, "9p"/"9 pm"→21:00, "2:30p"→14:30, "1400"→14:00, "12a"→00:00, "930pm"→21:30); empty clears; unparseable input flags the field (red border) instead of a browser popup; value shown back as friendly "9:00 AM" on blur. The meeting-log start-time field (`quick-log-dialog.tsx`) deliberately stays on the native input — out of scope (this was an actions ask). Docs (CLAUDE.md, ACTION-REMINDERS.md, schema comment) updated. **Verification caveat:** this fresh container couldn't install server deps (npm registry `ECONNRESET`), so the server `tsc` didn't run; the server edit is pure date arithmetic with **no new imports**, and its logic + the parser were verified by a standalone Node test (25 cases incl. weekday/weekend boundaries + invalid input, all pass). Client typecheck passed once the **pre-existing** tsconfig `baseUrl`-deprecation error (newer TS in the container, unrelated to this change) was bypassed; `prepush` backup guard green (32 tables). Vercel build is the real gate. Mobile (390px) not visually re-tested — a single text-input swap. |
| 2026-06-30 (s2) | **Meeting-log polish — caret stays in view + new actions on top (2 owner asks, schema-free, client-only, merged + pushed to `main` `a34f6aa`).** Both target the Quick Log / meeting editor. (1) **Caret no longer hides below the fold:** the note `MarkdownTextarea` auto-grows (`field-sizing-content`) so it never scrolls internally — it just extends past the bottom of the dialog/panel's scroll container, and the browser doesn't scroll that ancestor to follow the caret, so a line typed after Enter at the bottom landed out of view. Added `scrollCaretIntoView` (reuses the existing mirror-div `getCaretCoordinates` to find the caret's viewport Y, walks up to the nearest scrollable ancestor — `overflowY` auto/scroll with real overflow — and nudges its `scrollTop` with a 16px margin when the caret is past the top/bottom edge). Wired into the textarea `onChange` (covers plain typing/Enter) and into `apply`'s rAF (covers programmatic edits: list continuation, @-mention insert, pasted/dropped images). (2) **"Add action" prepends:** `addAction` in `quick-log-dialog.tsx` now does `[makePendingAction(), ...prev]` so the new composer row appears at the top, right under the button where it's easy to find; saving is key-based (`reconcileActions` dedups by row key) so row order is purely cosmetic. Owner confirmed both work live. Client-only, no schema/backup impact. **Verification caveat:** `npm run prepush` halts on the **pre-existing** tsconfig `baseUrl` deprecation (TS5101 — newer TS in this container, present on the untouched tree too); confirmed the two edited files type-check clean via `tsc --ignoreDeprecations 6.0` (exit 0). Developed on `claude/meeting-notes-improvements-tuvjxd`, merged `--no-ff` to `main` and pushed. Mobile (390px) not visually re-tested — a scroll nudge + a list-order flip, no layout change. |
| 2026-07-02 | **Duplicate auto-merge/dismiss bug hunt — found and fixed the actual root cause of the 2026-06-29 persistence fix not working (owner report via GitHub task, branch `claude/org-merge-dedup-issues-ddtn2r`).** Owner reported that, after the 2026-06-29 session, both original symptoms were still happening: a merged org ("ABC into ABC-D") kept reappearing as a fresh duplicate, and dismissed pairs kept coming back. Rather than trust the prior session's self-report, installed server deps in this fresh container (worked around two environment issues: `@prisma/engines`/`better-sqlite3` binary downloads failing with `ECONNRESET` via Node's built-in `fetch`/undici even through the proxy — fixed with `NODE_USE_ENV_PROXY=1` per `/root/.ccr/README.md`, plus a manual `curl` fetch of the one still-flaky file; and the documented `prisma db push`-from-`server/`-writes-to-the-wrong-`dev.db` gotcha) and ran the local server against a real SQLite DB to empirically reproduce the flow end-to-end rather than just re-reading the code. **Root cause (confirmed by direct repro):** both merge endpoints (`POST /duplicates/merge` and `/duplicates/companies/merge`) only wrote the `DuplicateMergeRule` row `if (removedKey !== keptKey)` — but `removedKey`/`keptKey` are the **normalized core name**, and for the single most common real-world duplicate shape (two names that normalize to the *identical* core string — exact re-entered duplicates, or a legal-entity-suffix variant like "Acme Health System" vs "Acme Health System Inc", both stripping to "acme health") those keys are **always equal**, so the guard silently skipped writing the rule on every such merge. The merge itself succeeded, but nothing was recorded to recognize the pair on a future reimport — confirmed by merging a live test pair, checking the `DuplicateMergeRule` table directly (empty), recreating the removed name, and rescanning (`autoMergedCount: 0`, pair shown again for manual review). Same guard existed on the contact side (bites suffix-only diffs like "Robert Chen" vs "Robert Chen Jr.", where `normalizeName` strips the suffix down to an identical key). **Fix** (`server/src/routes/duplicates.ts`): removed the guard — a rule is now always recorded, including the self-mapped case (`removedKey === keptKey === X`); the scan's auto-merge decision gained an explicit branch for `key1 === key2 && mergeRuleMap.has(key1)` that keeps the **lower id** (the more established record) and folds the other in, since a same-key rule can't otherwise tell which literal spelling is which. Also fixed a related correctness bug found in the same code path: the recorded `keptKey` was read from the **pre-merge** fetched `keep.name`, which goes stale if the merge's field-selection chose the *removed* contact/company's name for the survivor — now re-fetches the kept record's actual name after the merge completes. **Second, separate bug** (client, likely the real explanation for "dismissed pairs keep coming back"): `handleDismiss` in `client/src/pages/duplicates.tsx` fired the dismiss POST and optimistically removed the pair from the on-screen list **without awaiting the request**, silently swallowing any failure (`.catch(() => {})`) — unlike `handleMerge`, which does await + toast on error. Any transient failure (cold start, the documented stale-libsql-connection-on-warm-Vercel-instance issue) would look like a successful dismiss in the UI while persisting nothing, so it would reappear on the next scan with zero user-visible indication anything went wrong. Fixed to await + toast an error + only drop it from view on confirmed success, for both the contacts and companies tabs. **Verified empirically** (local server + curl, real SQLite): reproduced the original bug pre-fix (merge → `DuplicateMergeRule` table empty → recreate → `autoMergedCount: 0`); confirmed the fix (same flow → rule recorded → `autoMergedCount: 1`, correct lower-id survivor); regression-checked the pre-existing differing-core-key path (typo-similarity match, unaffected by the bug) still auto-merges correctly; verified the contact-side suffix case; verified the field-selection/stale-keptKey fix directly against the DB; re-verified dismiss-then-rescan (x2) still suppresses correctly (no regression — the server-side dismiss logic was already correct, so this arm of the complaint was pinned on the client bug instead). Client+server typecheck, `prepush` (backup guard, 32 tables — unaffected, no schema change), and full client `vite build` all green. **Schema-free.** Pushed to the task-assigned branch `claude/org-merge-dedup-issues-ddtn2r` first; **owner then asked to push to `main`** — fast-forwarded cleanly (`d712012..4112a85`, no divergence), live, Vercel auto-deploy triggered. |
| 2026-07-02 (s3) | **Reuse a series' prep notes in the next meeting (owner ask, schema-free, client-only, pushed `ef46ee0` → `5c66d4e` → `d0fcadb` to `main`).** When logging a meeting in a series, the desktop Quick Log "Last Meeting in Series" panel already showed the prior meeting's summary/notes/next-steps — owner wanted it to also let them **reuse the prior meeting's prep notes**. All in `client/src/components/quick-log-dialog.tsx` (the `/meetings` list `include` already returned `prepNotes`, so the series-context object already carried them — no server change). **(1, `ef46ee0`)** The panel now renders the last meeting's **prep notes** with a **"Copy to prep notes"** button (`copyPrepNotesFromSeries`) that duplicates their *content* into the new meeting as fresh, editable prep notes — a **one-way content copy** that never touches the prior meeting's own prep-note records (dated **today**, since they're prep for the *new* meeting). In create mode the copies stage as `pendingPrepNotes` (persisted on finalize like any staged note); once the record exists (edit mode, or after autosave) they `POST` to `/conversation-prepnotes` + reload. **(2, `5c66d4e`)** Owner follow-up: once copied, hide the source prep-notes box so the copies (now editable at the top of the panel) don't duplicate on screen and the last-meeting **notes** box gets the reclaimed vertical space. First cut keyed the hide on a session-only `copiedSeriesPrepId` state. **(3, `d0fcadb`)** Owner caught that copy → save → **reopen** re-showed the box (session state resets each open). Fixed by basing the box's visibility on a **durable** signal — `meetingHasPrepNotes = prepNotes.length > 0 || pendingPrepNotes.length > 0` (this meeting's own notes, saved or staged) — so it stays hidden after save+reopen, and re-appears only if all of this meeting's prep notes are cleared; removed the session state entirely. **Desktop-only** (the whole "Last Meeting in Series" panel is `useIsDesktop`-gated — mobile has no side panel, unchanged). `prepush` (typecheck + 32-table backup guard) + full client `vite build` green. Mobile not re-tested (no mobile surface changed). |
| 2026-07-02 (s2) | **Duplicate auto-merge, round 2 — the fallback itself had a gap, plus creation-time prevention added (owner follow-up: tested live, "not sure it's working").** Owner's concrete repro: merged company "NCQA" into "National Committee for Quality Assurance (NCQA)" (kept the latter), then created a new contact "xxxx" with org "NCQA" typed free-text — expected the org to resolve/correct to the full name "shortly after" creation; it didn't. **Two distinct gaps found, both empirically confirmed with a local server before and after the fix (curl against real SQLite):** **(1) The lazy Duplicates-page fallback from the 2026-07-01 fix never even considered this pair.** The scan's merge-rule check only ran on pairs the *heuristic* similarity scan (name similarity / token subset / shared prefix, for companies; token+email index for contacts) had already flagged as candidates — and "NCQA" vs "National Committee for Quality Assurance (NCQA)" shares no token and ~0% string similarity, so it's never a candidate at all (confirmed: scanning the two fresh, unmerged companies returned `pairs: []`). A merge rule is a *stronger*, explicit signal than the heuristic and must not depend on it. **Fix:** extracted `applyMergeRules()` (generic over contacts/companies) that runs as an **independent first pass** over *all* current entities — grouped by normalized key — matching every `DuplicateMergeRule` regardless of any similarity heuristic (handles both the same-key case from round 1 and, new, differing-key pairs with zero resemblance), before the heuristic scan runs at all; whatever it merges away is removed from the working list so the heuristic pass never re-sees it. `GET /api/duplicates[/companies]` simplified accordingly — the heuristic post-processing loop now only checks dismissals, since rules are fully resolved upstream. **(2) Nothing consulted merge history at company-*creation* time**, so the org still got recreated as a fresh "NCQA" duplicate the instant it was typed — the fallback (even fixed) only cleans up on a later Duplicates-page visit, not the "shortly after" the owner wanted. **Fix:** new exported `resolveExistingCompanyByName(name, preloadedCompanies?)` in `duplicates.ts` — exact case-insensitive name match first, else a `DuplicateMergeRule` redirect to the still-existing kept company, else `null` (caller creates). Wired into a new `POST /api/companies/resolve` (find-or-create, used by 5 client call sites that each had their own bare "look up locally, else `POST /companies`" pattern: the main contact-form company-entry flow, its LinkedIn-import company creation, actions' company tagging, ideas' company tagging, and the Quick Log meeting-org resolver) and directly into the CSV-import `resolveCompany` helper in `contacts.ts` (server-side, no HTTP round trip) — replacing its own bare exact-match-or-create logic — and into the standalone CSV-import-dialog's client-side per-row company lookup (a *third*, previously-unfixed CSV path, `csv-import-dialog.tsx`, which did its own `GET /companies` + create per uncached row). **Deliberately left untouched:** the standalone "Add Company" page and the org-`@`-mention "Create organization" button — both are explicit, conscious "make a new company" actions where silently redirecting to an existing one would be a surprise, unlike the other sites' implicit/incidental resolution. **Verified empirically end-to-end:** reproduced the exact NCQA scenario (merge → `/companies/resolve` for "NCQA" → returns the existing kept company, `created:false`, no duplicate ever created → contact attaches directly to the correct org); regression-tested the fallback-scan restructure (contact same-key suffix case, company/contact differing-key dissimilar-name case analogous to NCQA, heuristic-similarity-still-flagged-for-review case, dismiss-then-rescan×2) — all pass, no regressions. Client+server typecheck, `prepush` (32-table backup guard), full client `vite build`, and server `tsc` all green. **Schema-free.** Pushed to `main` directly (owner asked for that in round 1 of this thread; kept `claude/org-merge-dedup-issues-ddtn2r` in sync too, same as round 1). |
| 2026-07-02 (s4) | **Meeting-notes scroll-bar flicker fixed (owner report, schema-free, client-only, pushed `033673e` to `main`).** Owner: while typing meeting notes with SearchBook at **half-monitor width** (Teams in the other half), the right-edge scroll bar kept appearing and disappearing. **Root cause:** the notes `MarkdownTextarea` auto-grows (`field-sizing-content`) inside an `overflow-y-auto` scroll container; when it grows past the container a vertical scroll bar appears, which on classic (non-overlay) Windows scroll bars **consumes ~15px of width** → the narrower column **rewraps** the text → the textarea height changes by a line → content can drop back under the scroll threshold → scroll bar hides → width restored → rewraps back → grows again → oscillation read as flicker. Worse at half-monitor width because a narrow column sits right on word-wrap boundaries, so a 15px width delta readily flips a wrap. **Fix** (`client/src/components/quick-log-dialog.tsx`): added `[scrollbar-gutter:stable]` to the two scroll containers that wrap the notes field — the `DialogContent` (covers non-panel mode) and the desktop right-panel form scroll `div` (covers panel mode). Reserving the gutter permanently means toggling the scroll bar no longer changes content width → no rewrap → the feedback loop can't start; the bar appears once and stays. Minor intended cosmetic effect: a thin always-reserved gutter on the right of those panels. Client `tsc --noEmit` + `check:backup` (32-table guard) green; server `tsc` couldn't run in this container (no `node_modules` installed — pre-existing env limitation, all errors are missing `@types/node`/`@vercel/blob`/`@sentry/node`, none in the touched file), but the change is a two-line Tailwind className edit with no type surface and Vercel's `build:vercel` installs server deps before typechecking. **Schema-free.** Developed on `claude/meeting-notes-scroll-flicker-9lopxu`, fast-forwarded into `main` (`dd07481..033673e`) at owner's request; branch kept in sync. Owner confirmed the fix looks good. Mobile not re-tested (desktop-panel scroll behavior; mobile textarea sizing unchanged). |
| 2026-07-03 | **Owner UX polish batch + recurring-action reminder fix + weekday-only recurrence (owner asks, 5 commits `6646c88`→`63cc211` on `main`).** A run of small owner-facing tweaks plus two action-recurrence fixes (one schema). **Client polish, schema-free (`6646c88` / `8035c08` / `02c29b8`):** (1) **Dashboard pills decluttered** — the action **priority** pill now shows only when `HIGH` (Medium/Low were near-universal noise); the **type** pill is hidden when type is `OTHER`; and title + remaining pills/markers were moved onto **one wrapping row** (pills to the *right* of the name, wrapping under only when the title can't share the line) instead of a line below. (2) **Idea editor opens wider** — the Edit/New Idea dialog default `sm:w-[28rem]`→`sm:w-[52rem]` (still drag-resizable, capped `92vw`) so it isn't cramped on a large screen. (3) **Darker form-field outlines** — light-mode `--input` `oklch(0.922)`→`oklch(0.84)` (kept just below `--border`) because the near-white outline was hard to see, notably in Edge on the meeting-log documentation boxes; the change is at the token so it covers Input/Textarea/Select everywhere, and `Combobox`/`MultiCombobox` triggers (which fell back to the lighter `--border`) were given explicit `border-input` so the participant / org / series / tag pickers match. **Recurrence fixes:** (4, schema-free `876d3fc`) **reminder now carries onto recurring occurrences** — the next-occurrence creation in `PATCH /actions/:id/complete` copied schedule/priority/contacts but **dropped `dueTime` + `notify`**, so a recurring action lost its reminder after the first fire; now both carry forward and `lastNotifiedAt` is left null so the cron arms afresh. (5, **SCHEMA** `63cc211`) **weekday-only recurrence** — new additive `Action.recurringWeekdaysOnly` bool: when set, the next occurrence advances to the next weekday, skipping Sat/Sun (Fri→Mon), which a fixed day interval can't express. Form "Recurring action" block gained a **Repeat** selector (Every N days / Every weekday (Mon–Fri)); interval input hides in weekday mode; detail view + backup boolean-coercion updated; the new flag is carried onto recurrences too. Chose a clean dedicated column over a sentinel-in-`recurringIntervalDays` (owner picked "add a proper field" via AskUserQuestion) — the sentinel had real landmines (falsy-0 defeats the existing `&& recurringIntervalDays` gates; `-1` leaks into the "Every N days" display). **Local `prisma db push` + regenerate done; Turso DDL (`ALTER TABLE "Action" ADD COLUMN "recurringWeekdaysOnly" BOOLEAN NOT NULL DEFAULT false`) applied by the owner via the web SQL console** — I couldn't apply it myself because the committed rw token in `server/.env` is stale (hard 401); documented that in CLAUDE.md/AGENTS.md/NEXT-SESSION-PROMPT. `prepush` (client+server typecheck + 32-table backup guard) green on every commit; each pushed straight to `main` (owner's standing permission), schema commit held until the owner confirmed the DDL. Mobile (390px) not separately re-tested — dashboard row is a flex-wrap of existing chips, the rest are a dialog width + border-color + a form selector. |
| 2026-07-02 (s5) | **People/org picker relevance ranking + toolbar-less markdown in contact docs + @-mention keyboard-highlight fix (owner enhancement asks, schema-free, pushed `3391b43`→`18a698f` to `main`; owner confirmed live).** Three bundled owner requests. **(1) Relevance-ranked pickers** — the meeting **Participants** picker and the **@-mention** autocomplete (people *and* orgs) were unordered/alpha, so among several same-named people the intended one wasn't surfaced. `GET /api/contacts/names` and `/companies/names` now return a numeric `rank` and come back **sorted by it**. Rank is computed in app code (no schema) from cheap parallel `groupBy`s (the Turso-safe count pattern, **not** the `_count` include): the **primary** term is engagement — `min(meetings,40)*50 + min(@mentions,40)*30` (meetings = anchored `Conversation.contactId` + `ConversationParticipant`; @mentions = `ConversationMention` rows for that contact) — plus smaller boosts for NCQA ecosystem (+150) and having a written profile (+50, from an ids-only `OR` presence query over notes/personalDetails/usefulFor/roleDescription/openQuestions so the big text never transfers). Companies rank on `min(meetings,40)*50 + min(@mentions,40)*30 + min(peopleOnFile,20)*10` (meetings = anchor `companyId` + `ConversationOrg`). **Initial cut weighted NCQA at +1000** (all NCQA people sorted above everyone); owner then asked that **how often you meet/@-mention someone take precedence**, so it was reworked to the engagement-primary formula above (NCQA demoted to a tie-breaking +150) — e.g. among five "Sarah"s at NCQA, the one added-as-participant/@-mentioned far more wins. Client consumers: `ComboboxOption` gained an optional `rank`; `Combobox`/`MultiCombobox` and the `MarkdownTextarea` mention list now sort matches **word-prefix-first** ("sar" → *Sarah* before *Ce·sar*), then `rank` desc, then alpha (no-rank comboboxes unchanged except the new prefix-first tiering, which is strictly better); the mention list relies on the server's pre-sorted order within a tier (V8 stable sort). `quick-log-dialog.tsx` passes `rank` through to `contactOptions`/`companyOptions`. **(2) Toolbar-less markdown in contact documentation boxes** — new `hideToolbar` prop on `MarkdownTextarea` keeps every shortcut (Ctrl+B/I, `-` bullets, Tab-to-nest, list continuation, image paste) but drops the toolbar row; **Role Description, Useful For, Personal Details** on the contact form switched to it (Notes already had the full toolbar), and their contact-detail read views now render markdown via `prep-note-markdown` (`personalDetails` was plain `whitespace-pre-wrap` — bold/bullets would have shown as raw `**`). **(3) @-mention keyboard highlight** — arrow/Enter/Tab selection already existed but the highlighted row used `bg-accent` (`oklch(0.97)`, ~3% contrast on the white popover): barely visible in Chrome, **invisible in Edge** (both Chromium — the difference was display gamma, not CSS). Swapped to an explicit blue (`bg-blue-100`/dark-mode variant) matching the mention-chip theme, and added `scrollIntoView({block:'nearest'})` so the active row stays visible paging down a long list. **Verified:** `prepush` (client+server typecheck + 32-table backup guard) and full client `vite build` green; both ranking endpoints smoke-tested against the local SQLite server (curl) — `rank` present on all rows, correctly sorted desc, the new `ConversationMention` `groupBy` runs without hanging (in local dev.db, which holds the old job-search data with no NCQA-ecosystem contacts, ordering is driven purely by meeting count as designed). **Schema-free, no Turso DDL.** Committed straight to `main` (owner's standing permission): the three features in `3391b43`, the Edge highlight fix in `18a698f`. Mobile not separately re-tested (owner tested live in Chrome + Edge; changes are a dropdown highlight, picker ordering, and form-field editor swaps — no layout change). |
| 2026-07-06 | **Meeting-log dialog: wider default + Ctrl-click a participant name keeps the log open (owner ask, schema-free, client-only, pushed `f1bb55d` to `main`).** Two small owner-facing tweaks to the Quick Log / meeting editor (`client/src/components/quick-log-dialog.tsx`). **(1) Wider default width** — the non-prep-panel `DialogContent` width was `sm:w-[36rem]` (576px), narrower than the Ideas dialog; bumped to `sm:w-[52rem]` (832px, "like Ideas" per the owner). Panel mode (prep notes / series context shown) stays at the wider `sm:w-[64rem]`, and the dialog is still drag-resizable + `sm:max-w-[95vw]`-capped. **(2) Ctrl-click keeps the log open** — the participant-name `<Link to={/contacts/:id}>` previously called `handleDialogOpenChange(false)` on *every* click, so a Ctrl/Cmd-click opened the contact in a new browser tab **and** closed the meeting log (the owner wanted to open the person's tab to document about them *while continuing* to document in the log). The `onClick` now bails out on a modified/non-left click (`e.metaKey || e.ctrlKey || e.shiftKey || e.altKey || e.button !== 0`) — react-router already skips client-nav on a modified click, so the browser opens `/contacts/:id` in a new tab and the dialog stays open; a plain left-click still navigates in place and flushes+closes as before. **Mobile unaffected** — both the width change (`sm:` breakpoint, ≥640px; mobile stays `w-[95vw]`) and the modifier-key guard (touch = plain click) are desktop-only in effect. `prepush` (client+server typecheck + 32-table backup guard) + full client `vite build` green. Committed straight to `main` (owner's standing permission). |
| 2026-07-04 | **Meeting search results expand into a full read-only detail view with highlighting (owner ask, schema-free, client-only, pushed `182885e`→`28fb55a` to `main`; owner confirmed live).** Owner: clicking a **meeting** result in global search just navigated to the `/meetings` list (showing *all* meetings) — wanted instead an **expanded card showing the meeting's full contents + prep notes with the search term(s) highlighted**. **New `client/src/components/meeting-detail-dialog.tsx` (`MeetingDetailDialog`)**: clicking a meeting search card (title or card body) opens a read-only dialog that **fetches the full record via `GET /conversations/:id`** — necessary because the `/api/search` response only carries 60-char match snippets, not the full `notes`/`nextSteps`/prep-note bodies — and renders summary, **notes**, **next steps**, **prep notes**, attachments, and related people/orgs/tags chips (links to contacts/orgs). **Search terms highlighted throughout**: plain text (title/summary/participant names) via the existing `HighlightedText`; *inside the rendered markdown* (notes/prep/next-steps) via the existing `highlightRehype` rehype plugin — to reach the markdown, `MentionableMarkdown` gained optional `highlightTerms`/`caseSensitive` props (applies the plugin when present; keeps its `@`-mention chips), mirroring how `idea-list.tsx` already highlights descriptions. `MeetingSearchCard` in `search.tsx` changed from a `<Link>` to opening the dialog (one page-level dialog instance driven by `openMeetingId`; both the "All" and "Meetings" tabs; tag chips keep their own click via a `closest('a,button')` guard). Added a screen-reader `DialogDescription` (also cleared a Radix a11y console warning). **Second commit (`28fb55a`), owner follow-up:** the first cut's "Open in Meetings" button linked to `/meetings?id=`/`?title=` (which just filtered the list) — replaced with an **"Edit meeting"** button that closes the detail view and opens the **canonical Quick Log editor for THAT meeting** via the app-wide `useQuickLog().openEdit(id)` (the search page renders inside `QuickLogProvider`), no navigation. **Verified live in-browser** (Chrome DevTools MCP against the local server + real SQLite): searched a term present in a meeting's notes → card shows a "View full meeting" button → dialog opens with the notes rendered as markdown and the term highlighted yellow → "Edit meeting" opens the Quick Log editor pre-loaded with the correct meeting; re-tested at **390px mobile** (dialog respects `max-w-[calc(100%-2rem)]`, header row fits, notes list + highlight intact); no console errors beyond a pre-existing PWA-meta deprecation. **Out of scope (noted):** command-palette meeting hits still navigate to the list; the prep-note highlight path is byte-identical to the verified notes path but the local DB has no prep notes to exercise it live. `prepush` (client+server typecheck + 32-table backup guard) + full client `vite build` green. Committed straight to `main` (owner's standing permission). |
| 2026-07-07 | **Dashboard action ownership quick-switch + waiting-items sink — dashboard + /actions list (owner asks, schema-free, client-only, pushed `e2f5a63` + `ab13a09` to `main`).** Owner works his actions list from the dashboard; when he does his part of e.g. "Reach out to John re X" the ball moves to John's court — he wanted a few-clicks way to flip ownership without opening the action form, plus untimed "waiting on someone else" items kept at the bottom of the Overdue/Today lists. **No schema change** — the Task-3 ownership model (`owedByMe` + `owerContactIds` → derived `direction`) already expresses it; this is pure UI. **(1) New `ActionOwnerSelect`** (`client/src/components/action-owner-select.tsx`, mirrors `ActionDateSelect`): an hourglass popover on every dashboard action row. Owned rows: hover-revealed trigger (always visible on mobile) → one-click hand-off to the action's **linked contact(s)** (2 clicks total for the canonical case), a relevance-ranked contact search (`/contacts/names`, module-level fetch cache), or **"Someone else — no name"** (owner usually can't tell the system who — `owedByMe:false, owers:[]` = unnamed waiting). Waiting rows: always-visible fuchsia trigger (+ visible "Waiting" text when no named ower carries the cue) → removable Me/ower chips, add-person search, one-click **"Take it back — I own it"**. Always PUTs BOTH `owedByMe` + `owerContactIds` (server `resolveOwers` defaults `owedByMe` to true if omitted — sending one field alone would corrupt the other). **(2) `waitingSink`** primary sort key in `todayActions` + `combinedOverdue`: untimed `WAITING_ON_THEM` items sort after everything else (timed items keep their clock position; within the sunk group the old date/priority order applies). Verified live in-browser (Chrome DevTools MCP), desktop + 390px mobile: linked-contact hand-off, unnamed hand-off (HIGH item dropped below a MEDIUM — sink proven), take-back, search+Enter keyboard pick, both lists re-sorting; console clean; test actions deleted after. **Follow-up ask same session:** the quick-switch added to the `/actions` **list page** too (slim icon-only "Ownership" column after Due Date on desktop via a new `hideLabel` prop — the list already carries its own "Waiting" title-badge; on mobile the trigger sits inline next to the date select, column hidden like the other mobile-hidden columns) — verified live desktop + 390px (hand-off → badge appears + Waiting-For filter picks it up; take-back confirmed at the data level; test action deleted). **Env fix along the way:** local `server/prisma/dev.db` had drifted ~4 sessions behind the schema (missing `dueTime`/`notify`/`lastNotifiedAt`/`recurringWeekdaysOnly`) so every Action read/write 500'd locally — synced additively with `npx prisma db push --url "file:C:/dev/personal/searchbook/server/prisma/dev.db"` (Prisma 7's `--url` flag sidesteps the documented stray-`server/dev.db` CWD gotcha; backup of the pre-sync file in the session scratchpad). `prepush` + full client `vite build` green. |
| 2026-07-07 (s2) | **Meetings "Upcoming only" filter + new actions default to due today (owner asks, schema-free, pushed to `main`).** (1) The `/meetings` list "Hide upcoming" Switch became a three-way Select (All meetings / Hide upcoming / **Upcoming only**), persisted as `?when=past|upcoming`; legacy `?hideUpcoming=1` still reads as past, and picking a mode deletes the legacy param. Server: new `onlyUpcoming=1` pushes `{ NOT: notUpcomingClause(today, now) }` — the literal Prisma negation of the hide clause, so the modes are exact complements of the "Upcoming" badge rule (same client-sent ET today/now; skipped if malformed; hideUpcoming wins if both sent). Curl-verified against local SQLite: 224 = 221 past + 3 upcoming with a backdated `today`; partition exact at same-day/EOB boundaries. (2) New actions default `dueDate` to the creation day (local `en-CA`) on all three creation surfaces — New Action page (edit mode untouched), command-palette quick-add (incl. resetForms), Quick Log follow-up composer (`makePendingAction`; untitled rows still ignored by `actionsDirty`, so the default alone never creates anything). Form + palette got a ghost X "Clear due date" button; the composer's dense 3-col row relies on the native picker Clear. `prepush` + full client `vite build` green. Browser not driven this session (chrome-devtools MCP profile conflict); server behavior curl-verified, UI swaps mirror adjacent controls. |
| 2026-07-10 | **Meeting-log links (real `Link` feature) + blue picker highlight + series-name-as-title (+ series-only save) + dropped title suggestions (owner asks; 4 commits to `main`, one SCHEMA with owner-applied Turso DDL).** Batch 1 (`407e22c`, schema-free): (a) blue `Combobox`/`MultiCombobox` active-row highlight + `cursor-pointer` (matches the `@`-mention picker; replaces near-white `bg-accent`, invisible in Edge); (b) `conversationDisplayName` falls back to the **series name** when a meeting has no title; (c) removed the meeting **Title** suggestions dropdown (plain `Input`; deleted `title-autocomplete.tsx` + its `/conversations/titles` fetch); (d) a first cut of "add links" as an inline markdown Insert-link toolbar button. Batch 2 (`d0ea327`, **SCHEMA**): owner clarified they wanted links as a **managed feature in the "Tags, prep notes, attachments & links" section**, not an inline button — so reverted (d) and wired meetings into the shared **`Link`** model: `Link.conversationId` + `Conversation.links` (`onDelete: Cascade`), `/api/links` conversationId filter, links in both meeting includes, a Links subsection in Quick Log (add/list/remove; live POST/DELETE once saved, staged + flushed otherwise), link chips in the meeting detail dialog + `/meetings` cards, and `undo.ts` cascade-capture for `link`→`conversation`. **Turso DDL applied by owner** via the web SQL console (`ALTER TABLE "Link" ADD COLUMN "conversationId" INTEGER REFERENCES "Conversation"("id") ON DELETE CASCADE`); pushed only after confirmation. Verified end-to-end in-browser (Chrome DevTools MCP against local SQLite): add link → persists (in list + detail includes) → **delete meeting cascades the link away → Undo restores meeting + link with original ids**; all test data cleaned up. Batch 3 (`059618b`, schema-free bug-fix — owner report "saving a series meeting without a title still shows the toast"): a set series now also satisfies the server `hasWho()` + client save-validation gate (autosave/meaningful-content/finalize), not just the display fallback — a series-only meeting saves cleanly (curl-verified POST with only `seriesId`+date → 201; UI: pick series, no title, Done closes with no toast). **Local-DB gotcha hit + documented:** `prisma db push` from `server/` wrote the new column to the stray `server/dev.db`; the runtime opens `server/prisma/dev.db` (db.ts resolves relative to `prisma/`) → 500s until re-pushed with `DATABASE_URL="file:./prisma/dev.db"`. `prepush` (client+server typecheck + 32-table backup guard) green on every commit; blue-highlight + series-save also driven live at desktop width. |
| 2026-07-10 (s2) | **Ideas now show created + last-updated (owner ask; 1 feature commit `83c6a8a` to `main`, SCHEMA with owner-applied Turso DDL).** The `Idea` model only tracked `createdAt` — no update timestamp existed at all. Added **`Idea.updatedAt DateTime? @updatedAt`** (nullable on purpose so the column drops onto the already-populated table via a plain additive `ALTER`, no rebuild; Prisma auto-maintains it, so the existing PUT + archive PATCH bump it with **zero route changes**). New shared **`renderTimestamps()`** in `idea-list.tsx` (both card + list views): **"Created {date}" always**, plus a dimmed/italic **"· Updated {date}" only when the edit falls on a different local calendar day** (same-day tweaks would just repeat the date → suppressed to stay clean); a **hover tooltip always carries both exact timestamps with the time**. Each date is atomic (`whitespace-nowrap`) with a wrap allowed *between* them so a 390px card can't overflow; list view keeps its `hidden sm:inline` date column + an outer nowrap for a single desktop line. Client `Idea` type gained `updatedAt?: string \| null`; backup unchanged (`Idea` round-trips full records). **Turso DDL applied by owner** via the web SQL console: `ALTER TABLE "Idea" ADD COLUMN "updatedAt" DATETIME;` + backfill `UPDATE "Idea" SET "updatedAt" = "createdAt" WHERE "updatedAt" IS NULL;`; pushed only after confirmation. **Behavioral note:** archiving bumps `updatedAt` (it's a `.update()`) — left as-is, flagged to owner. **Local-DB gotcha re-hit:** `prisma db execute` from `server/` wrote the column to the stray `server/dev.db`, not the runtime's `server/prisma/dev.db` (→ 500s once the client regenerated); fixed by ALTERing the correct file directly via better-sqlite3. `prepush` (client+server typecheck + 32-table backup guard) + full `npm run build` green. **Browser not driven** (chrome-devtools MCP couldn't attach — profile in use); instead verified the endpoint returns `updatedAt` (curl) and unit-checked the created-vs-updated render branches incl. the UTC/local-day boundary in Node against `America/New_York`. |
| 2026-07-09 | **Vercel-exit contingency plan written (owner ask, docs-only, no code changes).** NCQA IT flagged Vercel as an "AI system" risk; owner asked for the best **free** hosting alternative (single user, immediate performance) plus a detailed just-in-case migration plan a less powerful model could execute. Audited every Vercel touchpoint in the codebase (serverless wrapper `api/index.ts`; `vercel.json` rewrites + the **daily-backup Vercel cron**; `@vercel/blob` in `upload.ts` + `backup.ts`; Blob URLs stored in DB columns *and* markdown note text; env inventory; dev-only `/photos` static serving; the generated Prisma client's platform-specific engine binary) and verified 2026 free-tier realities by web research (Cloud Run 2M req + 180k vCPU-s; Scheduler 3 free jobs; GCS 5GB; Cloudflare free = 10ms CPU cap; Render free = 15-min spin-down + 30–60s cold starts; Fly free tier dead). **Decision: Google Cloud Run + GCS + Cloud Scheduler, keep Turso** — the backend already runs as a plain Express server, so the port is a mechanical blob-storage swap + static serving + Dockerfile, and the every-minute reminders cron keeps the instance warm for free (request-based billing doesn't charge idle non-min instances). Wrote `.planning/VERCEL-EXIT-PLAN.md`: 6 phases (env-gated code changes safe to deploy to Vercel in parallel → GCP provisioning with $1-budget + max-instances-1 guardrails → blob object copy + all-text-columns URL rewrite scripts (with `--undo`) → Scheduler cutover + per-device PWA/push re-enrollment → Vercel decommission), each with exact commands, complete code snippets, verification gates, and rollback notes. **Status: contingency only, not started; nothing deployed or changed in the app.** |
| 2026-07-13 | **@-mention search (scope + `@` picker) and a green "happening NOW" marker on meetings (owner asks; 2 feature commits `29047b8` + `861a3f7` to `main`, the second SCHEMA with owner-applied Turso DDL).** **(1) Find every @-mention of a person/org from global search** (`29047b8`, schema-free — the `ConversationMention` index already existed, derived from note text on every save). Before, search reached mentions only *by accident*, matching the raw markdown token inside `notes` — producing snippets full of `[@Ann](/contacts/7)` and unable to tell "she was called out with `@`" apart from "her name appears in a sentence". Added a **`mentions` scope** to `/api/search` querying the index: a hit is a **meeting**, carrying **only the mentions that matched** (not everyone else it names), plus the note text around them (client reuses `mentionSnippets`, which knows not to slice through a token — so the payload ships note text rather than duplicating that windowing logic server-side). **All terms must land on ONE mention row**, so "Anne Smith" does not match a meeting that separately mentions "Anne Jones" and "Bob Smith" (verified with a seeded decoy). Mentions match on the name **as typed** AND on the linked record's **current** name, so a renamed contact stays findable. **Owner follow-up mid-session — "should I use the at-symbol in search? the dropdown would help me find the exact spelling"** → typing **`@` opens a picker** fed by a new **`GET /api/mentions/index`** (distinct mentioned entities + meeting counts, aggregated in JS — no `groupBy`/`_count`, per the Turso gotcha). Deliberately sourced from the **mention index, not the contact list**: every option is guaranteed ≥1 hit, and **loose names** (mentioned but never made contacts — precisely the ones you can't spell from memory) appear marked "not in CRM". Picking one strips the typed `@…` and pins a removable chip → `?mention=<key>`: `contact:440`/`company:5` (**bound, id-based** → survives a rename, keeps same-named people distinct) or `person:<name>`/`org:<name>` (loose). **A pinned target forces the mentions scope server-side** (the other scopes can't answer "who was @-mentioned", and with no text terms they'd match every record in the CRM); words typed afterwards then narrow the **meeting's own text** rather than searching for a name. ⚠ **Loose targets can only match by name, and Prisma's `equals` is case-sensitive on SQLite** → the clause must use `contains`, which **over-matches a longer name** ("Anne Marie Smith" ⊂ "Anne Marie Smithson"); both the rows **and the count** are re-verified in app code (`mentionMatchesTarget`) — proven with a seeded `Smithson` decoy (returns 3, not 4). Also **humanized mention tokens** (`@Ann`, not raw markdown) in meeting search snippets, and refactored the existing Mentions review page onto the now-shared `MentionChip` + `meetingMentionSnippets`. **(2) Meetings happening RIGHT NOW get a green border** (`861a3f7`, **SCHEMA**). Owner asked for a new colour for in-progress meetings — but the app **could not answer the question**: `Conversation` stored only a `startTime`, and "happening now" requires knowing when a meeting **ENDS**. Surfaced the gap and the two options (assume a fixed duration vs. store a real end time); **owner chose the real end time**. Added **`Conversation.endTime`** (local `HH:MM`) — and the **ICS parser was already computing the `DTEND` instant and throwing it away**, so Outlook-imported meetings now get exact end times for free; Quick Log gained an end-time field beside the start; the list shows the range ("7:12 PM–8:12 PM"). In-progress meetings get an **emerald left border + pulsing "Now" pill**, taking precedence over the sky-blue "Upcoming" (mutually exclusive by construction — "upcoming" already requires `startTime > now`). **Null `endTime` → assumed 60 min**, with the tooltip admitting the guess; an **untimed** meeting can never be "now". An ICS event crossing midnight stores `endTime: null` (a single-day record can't hold an end earlier than its start). The list re-renders on a **30s tick** (`useClockTick`) — a "right now" marker that only updates on reload isn't one. **Owner applied the Turso DDL** (`ALTER TABLE Conversation ADD COLUMN endTime TEXT;`) and confirmed it live before the push. **Verification:** both features driven live in-browser (Chrome DevTools MCP against local SQLite) with seeded data + deliberate decoys, then **all test data deleted** (local DB back to its original 224 meetings / 0 mentions). "Now" was proven against the **real clock**: a meeting seeded to end 60 seconds out **went dark on its own, on an untouched page**, while one running until 8:12 stayed lit. `prepush` + **full `npm run build`** green; **390px mobile re-tested** on all three surfaces (meetings list, Quick Log date/time row, `@` picker) — caught and fixed a cosmetic regression where the two time inputs at `6.75rem` clipped the AM/PM (restored to `7.5rem`). **Local-DB gotcha re-hit a THIRD time** (`db push` from `server/` wrote `endTime` to the stray `server/dev.db`, not the runtime's `server/prisma/dev.db` → every write 500'd with "column endTime does not exist") — this time **promoted the warning out of `NEXT-SESSION-PROMPT.md` into `CLAUDE.md`** (auto-loaded every session; the handoff file is not), with the `--url` fix inline. |
| 2026-07-16 | **Four owner UX asks — all schema-free, client-only, each merged to `main`.** **(1)** After pasting a recipient list into a meeting (Quick Log Participants), participants now sort **ascending by first name** instead of arriving in whatever order Outlook/email supplied — sort applied in `handleBulkPasteParticipants`'s `setParticipantIds` updater over the merged id list, resolving each name from `contactOptions` + the resolve-participants results (`quick-log-dialog.tsx`, `1fc9cec`→`e22d39d`). **(2)** The **"New Action" button was scrolling off-screen** on the Actions + Dashboard pages: root cause was **horizontal** overflow, not vertical — `SidebarInset` (a flex child of the sidebar row in `ui/sidebar.tsx`) had no `min-w-0`, so a wide table (the Actions list) forced the whole content column wider than the viewport, pushing the right-aligned header/action buttons past the right edge. Fix scoped to the app layout: `<SidebarInset className="min-w-0">` in `layout.tsx` — the column now shrinks to the viewport and wide tables scroll inside their own `overflow-x-auto` box (the shadcn `Table` already wraps in one) instead of pushing the page. Covers every wide page at once (same commit as #1). **(3) Faster meeting-time entry** (`c5881ee`→`0391e97`): replaced the two native `<input type="time">` fields (segmented on desktop, scroll-wheel on phone) with a new **`MeetingTimeRange`** component (`client/src/components/meeting-time-range.tsx`) tuned to how meetings actually look — on/half-hour starts, 30/45/60-min lengths, end after start. **Start** reuses the forgiving free-text `TimeInput` from action reminders (`"9"`→9:00, `"930"`→9:30, `"2p"`→2:00 PM); **End** is normally set with a **single tap on a `30m`/`45m`/`1h` duration chip** computed from the start (no end typing), chips disabled until a start exists + highlighting the matching span + tapping the active chip clears the end; the End field stays editable, and a typed end at/before the start is **bumped 12h** (`start 1:00, end "2"` → 2:00 PM) so it's always after the start; changing the start **preserves the chosen duration** (end shifts with it). Also added `startTime`/`endTime` to the Quick Log **autosave deps** — a time-only edit didn't trigger a save before. Owner confirmed "works great." **(4)** After **creating** a new action, navigate to the **`/actions` list** instead of the new action's detail page (`action-form.tsx`, `18fe989`→`7a40f24`); an **edit** still returns to the detail page (where the edit was launched). Verification: `client tsc` + `check:backup` (32 tables) green on each; server `tsc` green after installing server deps + `prisma generate` in the fresh container; the `MeetingTimeRange` duration/bump math unit-checked in Node (e.g. `13:30 + 45m → 14:15`, `start 13:00 / end "2" → 14:00`). Browser not driven this session (no local server/DB stack up); components are self-contained, owner testing on the live deploy. All four developed on `claude/meeting-imports-action-button-h0vtu9`, merged `--no-ff` into `main`. |
| 2026-07-16 (s2) | **LLM search-agent markdown export added as a third manual-backup file, + an agent guide (owner ask; schema-free, client-only; 3 commits to `main`).** Owner points an LLM agent at his backup to search/synthesize his notes and worried the JSON was growing too fast to stay usable. **Diagnosed first (not a bug):** compared his 6/16 vs 7/16 backups (0.83MB→2.04MB), found ~⅓ is `JSON.stringify(…, null, 2)` pretty-printing and ~half the real growth is a one-time NCQA network-onboarding wave (170 contacts / 309 companies / 514 employment rows), not a run rate; no pathology. The real issue was **shape, not size** — the JSON is a hostile agent target (notes are single escaped lines; attendees are bare integer IDs). **Built** `client/src/lib/llm-export.ts` (`buildLlmExport`) + wired it into `settings.tsx handleBackup` to download a third file, `searchbook-notes-<stamp>.md`, beside the JSON + binaries ZIP — a markdown **prose corpus**: Meetings (newest-first, per-attendee takeaway lines) / People (profiles + dossier prep notes) / Organizations / Ideas (archived included + labeled `(archived)`), every contact/company/tag/series ID resolved to a name, note markup cleaned (`[@Name](/contacts/7)`→`@Name`, `![screenshot](blob-url)`→`[image]`). ~550KB vs the 2MB JSON, greppable line-by-line, self-contained records; not a restore source. **First cut shipped only Meetings/People/Orgs; the owner's "…or ideas?" probe caught that Ideas + contact/company prep notes (~59KB of prose) were silently dropped** — folded all three in so the `.md` is the *complete* prose corpus, removing a silent cross-file miss (an agent asked "legal risks in meetings or ideas" would otherwise answer from meetings alone). **Wrote** `.planning/SEARCH-AGENT-GUIDE.md` — routing rule (prose/themes/ideas/notes → `.md`; exact numbers/dates/history → JSON; image contents → ZIP), the `.md` structure/schema, conventions (`@Name`, `[image]`, per-attendee takeaways, owner shorthand), how to answer common queries — cross-linked from `BACKUP-SCHEMA.md` (JSON, now marked the fallback) + indexed in `.planning/README.md`. `client tsc` + `check:backup` (32 tables) + full `npm run build` green; verified by transpiling `llm-export.ts` and running it against the owner's real 7/16 backup (25 ideas incl. 2 archived, 24 prep-note blocks, 0 residual blob URLs). Commits SSH-signed (a stop-hook flagged the first as Unverified → `--amend --reset-author` to sign, force-with-lease'd `main`). Browser not driven (pure data transform, exercised directly against real data). **Left as options (owner not asked):** merge the two CMS company records so a "CMS" grep can't miss anyone; later feed this export into the adaptation plan's Task 6.2 (semantic search over meeting notes). |
| 2026-07-21 | **@-mention mis-classification recovery + search count-skip speedup (2 owner asks, schema-free, 3 commits to `main` `121278d`/`f745a15`/`c7df6f3`).** **(1) A first-time `@`-mention accidentally tagged as an org can now be made a contact (and vice versa).** Slipping on "organization" instead of "person" in the `@`-picker writes a `[@Name](#org-mention)` token → a **loose org mention** whose only recovery was a "Create" button that made an *org*. **Server** (`server/src/routes/mentions.ts`): `create-contact` / `create-company` now accept a loose mention of **either** kind and rewrite **both** loose token forms (`#mention` *and* `#org-mention`) → the bound token, so the note stops re-deriving the wrong kind on the next save; each still rejects a mention already bound to a real record (converting a *created* org↔contact is a different destructive op, out of scope). This also closes a latent bug: `create-contact` had only ever rewritten `#mention`, so calling it on an org-mention would have silently made an orphan contact. **Client** (`client/src/pages/mentions.tsx`): the loose "Create" became a **split button** — primary click keeps the one-click default for the common (correct) case, a caret opens "Create as contact"/"Create as organization" for either type; follow-up made the control **hide optimistically the instant a type is chosen** (was lingering with a spinner through the round-trip + reload), restored only on error. **(2) Global search sped up ~4× by skipping redundant COUNT queries** (`c7df6f3`, `server/src/routes/search.ts`). Owner reported search felt slow and asked if a recent change caused it — **it did not** (this session never touched search; the only recent search-path change was the 2026-07-13 `@`-mentions-in-search feature). **Confirmed with the owner's Vercel `[TIMING]` logs instead of guessing**, which **overturned my first hypothesis**: with-vs-without the `mentions` scope was only ~250–580ms of a ~5,500ms search. The logs showed a 2-result search (`"barbara"` → 2 contacts/1 company) *still* took ~5.3s — cost is **fixed overhead, not result volume**: each of ~7 scopes ran a `findMany` **and** a full `count()` (complex `OR` + relation sub-queries) as a second sequential wave over Turso (~14 round-trips regardless of matches), plus cold-start/Turso-warmth variance (same "evan" query 1.5s cold vs 3.6s warm — infra, not code). **Fix:** in the `totals` block, **skip a scope's `count()` when its page came back short (< `take`)** — a short page already is the whole result set, so its total is the fetched length; a narrow search now runs **zero** count queries. **Pure speedup, zero behavior change** (when `take` didn't truncate, fetched length == `count()`; mentions uses the JS-verified length, matching `countMentionMeetings()` for the loose-`@`-target case). Deliberately **did not** speculatively overlap counts with fetches — once short pages skip, that would only waste queries on the narrow searches being sped up. **Confirmed in prod logs:** `/api/search` for "barbara"/"bar" went ~5,082–5,964ms → **~1,303–1,556ms**. Next lever if ever wanted: trim eager relation loads in the result fetches (`prepNotes` take 20, `participantInConversations` take 50). `prepush` + full `npm run build` green; **no Turso DDL** (client+server code only). Browser not driven from the container; the search change was **verified against production** via the owner's before/after `[TIMING]` logs. |
| 2026-07-20 | **LinkedIn import "About" moved out of Notes into a dedicated field (owner ask; SCHEMA — owner applied the Turso DDL before the push, one commit to `main`).** The LinkedIn import used to drop the profile's *About* text into `notes`, colliding with the owner's own notes. Added additive **`Contact.linkedinAbout`** (`ALTER TABLE "Contact" ADD COLUMN "linkedinAbout" TEXT`); the import dialog now routes the parsed `about` there (`contact-form.tsx` `onImport`), leaving `notes` untouched. A read-only **"About (LinkedIn)"** card on the contact detail (`contact-detail.tsx`) and an editable/clearable section on the contact form render **only when the field is non-empty** — i.e. only after an import — so manually-created contacts don't show an empty box. Rendered as **verbatim pre-wrapped plain text** (not `ReactMarkdown`) to avoid mangling stray `*`/`#`/`-` in LinkedIn copy. The import dialog's field-merge conflict for About now compares the parse against a prior `linkedinAbout` (not `notes`), labeled "About (LinkedIn)" (`linkedin-import-dialog.tsx`). Also folded into the LLM markdown export (`llm-export.ts`, "About (LinkedIn)" block) so the text stays available to the search agent (it previously rode along inside notes). **Forward-only** — existing contacts whose About sits in `notes` are not migrated (owner declined a backfill). Backup unchanged (both paths pass all columns; `Contact` already covered). Client `Contact` type + the draft-contact literal in `contact-list.tsx` gained the field — a **required** field there broke an `as Contact` cast that only the full `vite build`/`tsc -b` caught, not `prepush`'s `tsc --noEmit` (reaffirms the AGENTS.md "run a full build before pushing" rule). `prepush` + full `npm run build` green; browser not driven (a data-shaping change; a live import needs an OpenAI key). |
| 2026-07-22 | **Netlify migration Phases 1 & 2 executed — app LIVE on `ari-search-book.netlify.app` in parallel with Vercel (all on branch `claude/netlify-migration-plan-8lim9k`, NOT `main`).** Per `NETLIFY-MIGRATION-PLAN.md` (Phase 0 spike was already merged). **Phase 1 (additive, env-gated on `netlifyBlobsEnabled()` = `STORAGE=netlify` or runtime `NETLIFY`, so the same commits are dormant on Vercel/local):** storage abstraction over Netlify Blobs (`server/src/lib/storage.ts`, dynamic-imports `@netlify/blobs`), three-way upload branch (Netlify Blobs → Vercel Blob → local disk, relative `/photos`·`/files` paths), media proxy (`routes/media.ts` `GET /photos/:name`+`/files/:name`, mounted at root outside the `/api` gate), backup `/cron`+`/list` Netlify branches + authed `GET /backup/download/:name`, client `api.downloadBlob()` + a Settings download button for private `/api` backup URLs (plain anchor kept for Vercel), the serverless-http function entry (`netlify/functions/api.ts`) + root `netlify.toml` (redirects `/api`·`/photos`·`/files`→function, SPA fallback) + `build:netlify`, and **Prisma switched to the engine-less client** (`server/prisma.config.ts` `engine: "client"` — no Rust binary to bundle; runtime-equivalent via the driver adapters already in use; generated client is gitignored/regenerated per build so it's parallel-run safe). Verified locally: typecheck, `check:backup` (32 tables), full client build, engine-less `prisma generate`, and a runtime `SELECT 1` against the local better-sqlite3 adapter. **Phase 2:** owner created the Netlify site (production branch = the migration branch), set env (`STORAGE=netlify`, `APP_PASSWORD`, `TURSO_*`, `OPENAI_API_KEY`, `CLIENT_URL`, `REMINDERS_CRON_SECRET`; **no** `BLOB_READ_WRITE_TOKEN`; VAPID + `CRON_SECRET` deferred to Phase 5). **Six Netlify-runtime bugs surfaced live and were fixed on the branch** (each a real-serverless/relative-path assumption Vercel never exercised): (1) 502 `@prisma/client/runtime/client` missing → **hoist `@prisma/client`+`@prisma/adapter-libsql`+`@libsql/client` to the ROOT `package.json`** (function is at repo root; Netlify externalizes + resolves those from the function root, not `server/node_modules`); (2) 502 `@libsql/linux-x64-gnu` missing → **externalize `@libsql/client`+`@prisma/adapter-libsql`** in `netlify.toml` (native binding via a dynamic require esbuild can't trace); (3) 502 `ENOENT mkdir /var/task/data/photos` → **guard the local-dir mkdir** on `!isProduction && !netlifyBlobsEnabled()` (read-only FS, crashed at import); (4) 500 on `/photos/*` → **`connectLambda(event)`** in the function entry (serverless-http's Lambda signature blocks Netlify Blobs' auto-context; re-exported via `server/src/lib/netlify-blobs-context.ts` to share one `@netlify/blobs` instance with `storage.ts`); (5) images served corrupted → **serverless-http `binary` content-type allow-list** (it utf8-encodes responses by default); (6) photo uploads never displayed despite a success toast → **render relative `/photos` paths in production** (`photo-upload.tsx`, `contact-detail.tsx`; the old `import.meta.env.DEV ? photoFile : null` guard was a Vercel-era assumption that hid Netlify's relative paths so the `<img>` was never requested). Gate GREEN: `/api/health` ok, auth 401s unauthenticated, SPA serves, Blobs proxy reachable; owner confirmed in-browser login/contacts/**photo upload+render**/paste/manual-backup (3 files). **Not pushed to `main`** — the parallel-run guarantee keeps `main`→Vercel the untouched daily driver until Phase 5 cutover. Next: Phase 3 parallel soak. Open: VAPID+cron unset on Netlify (Phase 5), soak upload-path caveat (relative paths render on Netlify only), >~4.5 MB photos vs the ~6 MB base64 response cap, free-compute quota (R10) not yet eyeballed. |
