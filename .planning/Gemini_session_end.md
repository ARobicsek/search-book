Before ending the session:

1. Update the STATUS line of every task you touched in .planning/NCQA-ADAPTATION-PLAN.md (date, commit hash, any deviations from the plan).
2. Update .planning/NEXT-SESSION-PROMPT.md with:
   - What was completed
   - What's next
   - Any open bugs
3. If you made a durable decision (schema shape, taxonomy, rejected alternative), add a row to the decisions table in .planning/STATE.md.

Then commit and push your work (pushing to main is authorized; run `npm run prepush` first).
