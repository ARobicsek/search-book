{
  "name": "searchbook",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight local CRM for executive job search networking",
  "scripts": {
    "start": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev": "npm start",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "build:vercel": "npm install --prefix client && npm install --prefix server && (cd server && DATABASE_URL=file:./dev.db npx prisma generate) && npm run typecheck && npm run build --prefix client",
    "typecheck": "npm run typecheck --prefix client && npm run typecheck --prefix server",
    "prepush": "npm run typecheck && echo '✓ TypeScript check passed'",
    "db:migrate": "cd server && npx prisma migrate dev",
    "db:seed": "cd server && npx prisma db seed",
    "db:studio": "cd server && npx prisma studio"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "concurrently": "^9.1.2"
  }
}
